<?xml version="1.0" encoding="utf-8"?>
<CheatTable CheatEngineTableVersion="45">
  <CheatEntries>
    <CheatEntry>
      <ID>77</ID>
      <Description>"=== GGdropmans Grounded 2 V1.0 ==="</Description>
      <Options moHideChildren="1"/>
      <Color>0000C080</Color>
      <GroupHeader>1</GroupHeader>
      <CheatEntries>
        <CheatEntry>
          <ID>1</ID>
          <Description>"README — how to use"</Description>
          <Color>00808080</Color>
          <VariableType>Auto Assembler Script</VariableType>
          <AssemblerScript>{
        GGdropmans Grounded 2 Cheat Table V1.0
        ====================================
        Process: Maine-Win64-Shipping.exe (Steam)
        Companion to tools/grounded2-editor (Augusta / Grounded 2 save editor).
        Donate / tip: https://paypal.me/kd19902
        Discord: https://discord.gg/PTwyDTFyR

        1. Load into a world
        2. Attach CE to Maine-Win64-Shipping.exe
        3. Enable [ACTIVATE]
                4. Use Movement scripts for super speed / super jump / no-clip fly / teleport to aim (F6)
                5. Use Vitals / Economy / Settings for live cheats
                6. Gear → one-shot weapons / god armor scripts while items are equipped

                Offline unlocks (buildings, quests, analyze, fog, hatchery, buggy tier, OP preset, inventory dumps)
                stay in the browser save editor — use this .CT for live movement / vitals / world toggles.
                }
                [ENABLE]
                [DISABLE]
                </AssemblerScript>
        </CheatEntry>
        <CheatEntry>
          <ID>2</ID>
          <Description>"[ACTIVATE] register symbols"</Description>
          <Color>0000FF00</Color>
          <VariableType>Auto Assembler Script</VariableType>
          <AssemblerScript>[ENABLE]
        {$lua}
        if syntaxcheck then return end
        if getAddressSafe(process) == nil then
          showMessage("Attach to Maine-Win64-Shipping.exe (Grounded 2 / Augusta) first")
          error("no process")
        end

        -- Resolve GNames/UObject statics via AOB (same Maine UE signatures as G1 lineage).
        -- After a major Augusta patch, re-enable [ACTIVATE] or run DEBUG → Fetch Base Addresses.
        local aobList = {
          "4C 8B 05 ?? ?? ?? ?? 4D 85 C0 0F 84 ?? ?? ?? ?? 49 8B 80 40 01 00 00 48 89 9C 24 A0 00 00 00 48 85 C0",
          "4C 8B 35 ?? ?? ?? ?? 48 63 05 ?? ?? ?? ?? 4D 8D 24 C6 4D 3B F4",
          "48 8B 05 ?? ?? ?? ?? 48 89 3C D8 48 8B 9C 24 C0 00 00 00 44 88 A7 C0",
          "48 8B 05 ?? ?? ?? ?? 48 8B D1 48 8B 88 F8 0A 00 00 48 85 C9 74 07 48 8B",
          "48 8B 05 ?? ?? ?? ?? 49 8B D3 45 33 C0 48 8B 0A 48 39 81 80 02 00 00 74 11"
        }
        local aobNames = {"Player","Statistics","GearData","EngineData","GameState"}
        local xbase = getAddress(process)
        local defs = {}
        local miss = {}
        print("GGdropman Grounded 2: scanning AOBs on "..tostring(process).."…")
        for i = 1, #aobList do
          local aob = AOBScan(aobList[i], "+X*C*W")
          if aob == nil or aob.Count == 0 then
            miss[#miss+1] = aobNames[i]
            print(aobNames[i]..": MISS")
          else
            local instruct = getAddressSafe(aob[0])
            local distance = readInteger(instruct + 3)
            local instructSize = getInstructionSize(instruct)
            local address = (instruct + distance + instructSize) - xbase
            defs[aobNames[i]] = string.format("%s+%X", process, address)
            print(string.format("%s: %s", aobNames[i], defs[aobNames[i]]))
          end
          if aob then aob.destroy() end
        end
        if #miss &gt; 0 then
          showMessage("AOB miss: "..table.concat(miss, ", ").."\nPointer groups for those symbols will be empty until the signature is updated.")
        end
        if not defs.Player then
          showMessage("Player AOB failed — cannot activate. Game must be in-world (not main menu).")
          error("no Player AOB")
        end

        local aa = ""
        for _, n in ipairs(aobNames) do
          if defs[n] then
            aa = aa.."define("..n..","..defs[n]..")\n"
            aa = aa.."registersymbol("..n..")\n"
          end
        end
        autoAssemble(aa)

        -- Probe CharacterMovementComponent pointer on the player pawn (UE offset drifts on Augusta).
        gg2_cmc_off = nil
        local pawns = {0xDC8, 0xE00, 0xE08, 0xD80, 0xD00, 0xC80, 0xF00, 0x1080, 0x1100}
        local pRoot = readQword(getAddress("Player"))
        if pRoot and pRoot ~= 0 then
          for _, off in ipairs(pawns) do
            local c = readQword(pRoot + off)
            if c and c &gt; 0x10000 then
              -- Heuristic: CMC has MaxWalkSpeed-like floats in 0x140..0x360
              for foff = 0x140, 0x360, 4 do
                local v = readFloat(c + foff)
                if v and v &gt; 80 and v &lt; 2500 then
                  gg2_cmc_off = off
                  print(string.format("CMC candidate Player+0x%X (walk~%.0f @ +0x%X)", off, v, foff))
                  break
                end
              end
            end
            if gg2_cmc_off then break end
          end
        end
        if not gg2_cmc_off then
          gg2_cmc_off = 0xDC8
          print("CMC offset fallback Player+0xDC8 (move around and re-ACTIVATE if speed scripts fail)")
        end

        print("GGdropman Grounded 2 V1.0 activated")
        [DISABLE]
        {$lua}
        if syntaxcheck then return end
        for _, n in ipairs({"Player","Statistics","GearData","EngineData","GameState"}) do
          if getAddressSafe(n) then
            pcall(function() autoAssemble("unregistersymbol("..n..")") end)
          end
        end
        gg2_cmc_off = nil
        </AssemblerScript>
        </CheatEntry>
        <CheatEntry>
          <ID>460</ID>
          <Description>"Quick loadout"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00F0C14A</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>461</ID>
              <Description>"[Script] GOD LOADOUT (god + speed + oneshot + freebuild)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_loadout = gg2_loadout or {}
            local st = gg2_loadout
            st.prev = {}
            -- Free build pack
            for _, t in ipairs({
              {n="recipes", off={0x3A,0x138,0x388,0x170}},
              {n="freebuild", off={0x3B,0x138,0x388,0x170}},
              {n="recipesFree", off={0x3D,0x138,0x388,0x170}},
              {n="perks", off={0x49,0x138,0x388,0x170}},
              {n="bugsIgnore", off={0x3F,0x138,0x388,0x170}},
            }) do
              local a = resolve("GameState", t.off)
              if a then
                st.prev[t.n] = readBytes(a, 1, true)
                writeBytes(a, 1)
              end
            end
            -- Damage scale oneshot
            local dmg = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
            local edmg = resolve("GameState", {0x54, 0x138, 0x388, 0x170})
            if dmg then st.prev.dmg = readFloat(dmg); writeFloat(dmg, 100) end
            if edmg then st.prev.edmg = readFloat(edmg); writeFloat(edmg, 0.05) end

            -- Speed: find walk on CMC
            local p = readQword(getAddress("Player"))
            local c = readQword(p + (gg2_cmc_off or 0xDC8))
            st.walkOff = nil
            st.walkBase = 600
            if c and c &gt; 0x10000 then
              for off = 0x140, 0x360, 4 do
                local v = readFloat(c + off)
                if v and v &gt; 80 and v &lt; 2500 then
                  st.walkOff = off
                  st.walkBase = v
                  break
                end
              end
            end

            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              local dmg2 = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
              local edmg2 = resolve("GameState", {0x54, 0x138, 0x388, 0x170})
              if dmg2 then writeFloat(dmg2, 100) end
              if edmg2 then writeFloat(edmg2, 0.05) end
              for _, t in ipairs({
                {off={0x3A,0x138,0x388,0x170}},
                {off={0x3B,0x138,0x388,0x170}},
                {off={0x3D,0x138,0x388,0x170}},
                {off={0x49,0x138,0x388,0x170}},
                {off={0x3F,0x138,0x388,0x170}},
              }) do
                local a = resolve("GameState", t.off)
                if a then writeBytes(a, 1) end
              end
              local hd = resolve("Player", {0x284, 0x30, 0x1B8})
              local hb = resolve("Player", {0x280, 0x30, 0x1B8})
              local stam = resolve("Player", {0xD8, 0x150, 0x1B8})
              if hd then writeFloat(hd, 0) end
              if hb then writeFloat(hb, 200) end
              if stam then writeFloat(stam, 200) end
              local hung = resolve("Player", {0x278, 0x130, 0x1B8})
              local th = resolve("Player", {0x27C, 0x130, 0x1B8})
              if hung then writeFloat(hung, 5) end
              if th then writeFloat(th, 5) end
              local p2 = readQword(getAddressSafe("Player") or 0)
              if p2 and p2 ~= 0 and st.walkOff then
                local c2 = readQword(p2 + (gg2_cmc_off or 0xDC8))
                if c2 then writeFloat(c2 + st.walkOff, (st.walkBase or 600) * 5) end
              end
            end
            st.timer.Enabled = true
            print("GOD LOADOUT ON � god HP/stam/hunger + speed x5 + damage x100 + free build + bugs ignore")
            showMessage("GOD LOADOUT enabled.\nGod vitals + Super Speed x5 + One-shot damage + Free build.\nDisable this script to restore damage scales / toggles.")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local st = gg2_loadout
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.prev then
              local map = {
                recipes={0x3A,0x138,0x388,0x170},
                freebuild={0x3B,0x138,0x388,0x170},
                recipesFree={0x3D,0x138,0x388,0x170},
                perks={0x49,0x138,0x388,0x170},
                bugsIgnore={0x3F,0x138,0x388,0x170},
              }
              for n, off in pairs(map) do
                local a = resolve("GameState", off)
                if a and st.prev[n] ~= nil then writeBytes(a, st.prev[n]) end
              end
              local dmg = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
              local edmg = resolve("GameState", {0x54, 0x138, 0x388, 0x170})
              if dmg and st.prev.dmg then writeFloat(dmg, st.prev.dmg) end
              if edmg and st.prev.edmg then writeFloat(edmg, st.prev.edmg) end
            end
            if st and st.walkOff and st.walkBase then
              local p = readQword(getAddressSafe("Player") or 0)
              if p and p ~= 0 then
                local c = readQword(p + (gg2_cmc_off or 0xDC8))
                if c then writeFloat(c + st.walkOff, st.walkBase) end
              end
            end
            print("GOD LOADOUT OFF")
            </AssemblerScript>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>12</ID>
          <Description>"Movement (speed / jump / fly)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>0040C0FF</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>3</ID>
              <Description>"[Script] Super Speed x5"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player symbol")
            end

            gg_grounded_speed = gg_grounded_speed or {}
            local st = gg_grounded_speed
            st.mult = 5.0
            st.baseWalk = nil
            st.off = nil

            local function cmc()
              local p = readQword(getAddress("Player"))
              if p == nil or p == 0 then return nil end
              return readQword(p + (gg2_cmc_off or 0xDC8))
            end

            local function findWalk(c)
              -- Prefer floats that look like MaxWalkSpeed / MaxFlySpeed defaults.
              local targets = {600, 500, 450, 400, 350, 300}
              local bestOff, bestScore = nil, 999
              for off = 0x140, 0x360, 4 do
                local v = readFloat(c + off)
                if v and v &gt; 80 and v &lt; 2500 then
                  for _, t in ipairs(targets) do
                    local d = math.abs(v - t)
                    if d &lt; bestScore then
                      bestScore = d
                      bestOff = off
                      st.baseWalk = t
                    end
                  end
                end
              end
              return bestOff
            end

            st.off = findWalk(cmc())
            if not st.off then
              showMessage("Could not find MaxWalkSpeed near CharacterMovement. Try flying/moving first, then re-enable.")
              error("no walk float")
            end
            print(string.format("Super Speed: CMC+0x%X (base~%.0f) x%.1f", st.off, st.baseWalk or 600, st.mult))

            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              local c = cmc()
              if not c or not st.off then return end
              writeFloat(c + st.off, (st.baseWalk or 600) * st.mult)
              -- Also push nearby speed-like siblings (MaxWalkSpeedCrouched / MaxFlySpeed) if present.
              for _, d in ipairs({-8, -4, 4, 8, 12, 16}) do
                local v = readFloat(c + st.off + d)
                if v and v &gt; 50 and v &lt; 2500 then
                  writeFloat(c + st.off + d, math.max(v, (st.baseWalk or 600) * st.mult * 0.6))
                end
              end
            end
            st.timer.Enabled = true

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_grounded_speed
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            if st and st.off then
              local p = readQword(getAddressSafe("Player") or 0)
              if p and p ~= 0 then
                local c = readQword(p + (gg2_cmc_off or 0xDC8))
                if c and st.baseWalk then writeFloat(c + st.off, st.baseWalk) end
              end
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>4</ID>
              <Description>"[Script] Super Jump x4"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player symbol")
            end

            gg_grounded_jump = gg_grounded_jump or {}
            local st = gg_grounded_jump
            st.mult = 4.0
            st.baseJump = nil
            st.off = nil

            local function cmc()
              local p = readQword(getAddress("Player"))
              if p == nil or p == 0 then return nil end
              return readQword(p + (gg2_cmc_off or 0xDC8))
            end

            local function findJump(c)
              local targets = {420, 700, 800, 900, 1000, 350, 500}
              local bestOff, bestScore = nil, 80
              for off = 0x140, 0x380, 4 do
                local v = readFloat(c + off)
                if v and v &gt; 200 and v &lt; 3000 then
                  for _, t in ipairs(targets) do
                    local d = math.abs(v - t)
                    if d &lt; bestScore then
                      bestScore = d
                      bestOff = off
                      st.baseJump = v
                    end
                  end
                end
              end
              return bestOff
            end

            st.off = findJump(cmc())
            if not st.off then
              showMessage("Could not find JumpZVelocity. Jump once in-game, then re-enable.")
              error("no jump float")
            end
            print(string.format("Super Jump: CMC+0x%X (base~%.1f) x%.1f", st.off, st.baseJump or 420, st.mult))

            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              local c = cmc()
              if not c or not st.off then return end
              writeFloat(c + st.off, (st.baseJump or 420) * st.mult)
            end
            st.timer.Enabled = true

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_grounded_jump
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            if st and st.off and st.baseJump then
              local p = readQword(getAddressSafe("Player") or 0)
              if p and p ~= 0 then
                local c = readQword(p + (gg2_cmc_off or 0xDC8))
                if c then writeFloat(c + st.off, st.baseJump) end
              end
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>5</ID>
              <Description>"[Script] No-clip fly"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            gg_grounded_noclip = gg_grounded_noclip or {}
            local st = gg_grounded_noclip
            st.prevCollision = nil

            -- Snapshot collision so we can restore on disable
            do
              local col = resolve("Player", {0x64})
              if col then st.prevCollision = readBytes(col, 1, true) end
            end

            local function apply()
              local fly = resolve("Player", {0x3A8,0xDC8})
              local mode = resolve("Player", {0x178,0xDC8})
              local col = resolve("Player", {0x64})
              if fly then writeByte(fly, 1) end
              if mode then writeByte(mode, 5) end -- MOVE_Flying
              if col then writeByte(col, 0) end   -- NoCollision
              -- CharacterMovement: clear grounded / force flying-friendly flags when present
              local p = readQword(getAddress("Player"))
              if p and p ~= 0 then
                local cmc = readQword(p + 0xDC8)
                if cmc and cmc ~= 0 then
                  -- GravityScale-ish floats near movement: nudge toward 0 while noclip is on
                  for _, off in ipairs({0x170, 0x174, 0x178, 0x17C, 0x180}) do
                    local v = readFloat(cmc + off)
                    if v and v &gt; 0.2 and v &lt; 5.0 then
                      writeFloat(cmc + off, 0.0)
                    end
                  end
                end
              end
            end

            apply()
            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function() apply() end
            st.timer.Enabled = true
            print("No-clip fly ON (fly + collision off). Disable this script to restore walking/collision.")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            local st = gg_grounded_noclip
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end

            local fly = resolve("Player", {0x3A8,0xDC8})
            local mode = resolve("Player", {0x178,0xDC8})
            local col = resolve("Player", {0x64})
            if fly then writeByte(fly, 0) end
            if mode then writeByte(mode, 1) end -- MOVE_Walking
            if col then
              if st and st.prevCollision ~= nil then
                writeByte(col, st.prevCollision)
              else
                writeByte(col, 1) -- QueryAndPhysics-ish default
              end
            end
            -- Restore a normal gravity scale if we zeroed one
            local p = readQword(getAddressSafe("Player") or 0)
            if p and p ~= 0 then
              local cmc = readQword(p + 0xDC8)
              if cmc and cmc ~= 0 then
                for _, off in ipairs({0x170, 0x174, 0x178, 0x17C, 0x180}) do
                  local v = readFloat(cmc + off)
                  if v ~= nil and v == 0.0 then
                    writeFloat(cmc + off, 1.0)
                    break
                  end
                end
              end
            end
            print("No-clip fly OFF")
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>6</ID>
              <Description>"[Script] Fly mode (collision on)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local fly = resolve("Player", {0x3A8,0xDC8})
            local mode = resolve("Player", {0x178,0xDC8})
            if fly then writeByte(fly, 1) end
            if mode then writeByte(mode, 5) end -- MOVE_Flying
            print("Fly mode on (MovementMode=5) — collision still on. Use No-clip fly to phase through geometry.")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local fly = resolve("Player", {0x3A8,0xDC8})
            local mode = resolve("Player", {0x178,0xDC8})
            if fly then writeByte(fly, 0) end
            if mode then writeByte(mode, 1) end -- MOVE_Walking
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>7</ID>
              <Description>"[Script] Teleport to aim (F6)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            gg_grounded_tp = gg_grounded_tp or {}
            local st = gg_grounded_tp
            -- Distance in Unreal units (~cm). 2500 ≈ 25m. Raise for longer blinks.
            st.dist = st.dist or 2500

            local function looksPos(x, y, z)
              if x == nil or y == nil or z == nil then return false end
              if not (x == x and y == y and z == z) then return false end -- NaN
              local a = math.abs(x) + math.abs(y)
              -- Grounded yard coords are large; reject origin/ junk
              return a &gt; 500 and a &lt; 500000 and math.abs(z) &lt; 200000
            end

            local function readVec(addr)
              return readFloat(addr), readFloat(addr + 4), readFloat(addr + 8)
            end

            local function writeVec(addr, x, y, z)
              writeFloat(addr, x)
              writeFloat(addr + 4, y)
              writeFloat(addr + 8, z)
            end

            local function pawn()
              local p = readQword(getAddress("Player"))
              if p == nil or p == 0 then return nil end
              return p
            end

            -- Find writable world XYZ near the character (root / capsule / CMC).
            local function findLocAddr(p)
              if st.locAddr and looksPos(readVec(st.locAddr)) then return st.locAddr end
              local candidates = {}
              local function consider(addr, tag)
                local x, y, z = readVec(addr)
                if looksPos(x, y, z) then
                  table.insert(candidates, {addr = addr, tag = tag, score = math.abs(x) + math.abs(y)})
                end
              end

              local rootOffs = {0x130, 0x138, 0x140, 0x190, 0x198, 0x1A0, 0x1A8, 0x128, 0x120}
              local locOffs = {0x11C, 0x120, 0x128, 0x140, 0x1C0, 0x1D0, 0x1E0, 0x220, 0x250}
              for _, ro in ipairs(rootOffs) do
                local root = readQword(p + ro)
                if root and root &gt; 0x10000 then
                  for _, lo in ipairs(locOffs) do
                    consider(root + lo, string.format("root+%X loc+%X", ro, lo))
                  end
                end
              end

              local cmc = readQword(p + 0xDC8)
              if cmc and cmc &gt; 0x10000 then
                for _, lo in ipairs({0x2A0, 0x2B0, 0x2C0, 0x2D0, 0x300, 0x310, 0x320, 0x340, 0x360, 0x380, 0x3A0}) do
                  consider(cmc + lo, string.format("cmc+%X", lo))
                end
                -- UpdatedComponent → RelativeLocation
                for _, uo in ipairs({0x160, 0x168, 0x170, 0x178, 0x188, 0x190}) do
                  local upd = readQword(cmc + uo)
                  if upd and upd &gt; 0x10000 then
                    for _, lo in ipairs(locOffs) do
                      consider(upd + lo, string.format("upd+%X loc+%X", uo, lo))
                    end
                  end
                end
              end

              table.sort(candidates, function(a, b) return a.score &gt; b.score end)
              if #candidates == 0 then return nil end
              st.locAddr = candidates[1].addr
              st.locTag = candidates[1].tag
              print(string.format("TP loc @ %X (%s) = %.1f %.1f %.1f", st.locAddr, st.locTag, readVec(st.locAddr)))
              return st.locAddr
            end

            -- ControlRotation / camera POV → forward vector (UE degrees).
            local function findAim(p)
              local ctrlOffs = {0x260, 0x268, 0x270, 0x2A8, 0x2B0, 0x2B8, 0x2C0, 0x320}
              local rotOffs = {0x288, 0x290, 0x298, 0x3D0, 0x3D8, 0x408, 0x410}
              for _, co in ipairs(ctrlOffs) do
                local ctrl = readQword(p + co)
                if ctrl and ctrl &gt; 0x10000 then
                  for _, ro in ipairs(rotOffs) do
                    local pitch = readFloat(ctrl + ro)
                    local yaw = readFloat(ctrl + ro + 4)
                    if pitch and yaw and math.abs(pitch) &lt;= 90.5 and math.abs(yaw) &lt;= 360.5 then
                      -- Prefer non-zero yaw (looking around)
                      if math.abs(yaw) &gt; 0.01 or math.abs(pitch) &gt; 0.01 then
                        return pitch, yaw, "ctrl"
                      end
                    end
                  end
                  -- PlayerCameraManager → CameraCache POV
                  for _, cmo in ipairs({0x340, 0x348, 0x350, 0x2B8, 0x2C8, 0x438}) do
                    local cam = readQword(ctrl + cmo)
                    if cam and cam &gt; 0x10000 then
                      for _, po in ipairs({0x1BA0, 0x1BB0, 0x1A90, 0x1AA0, 0x22B0, 0x22C0, 0x10, 0x20}) do
                        local lx, ly, lz = readVec(cam + po)
                        local pitch = readFloat(cam + po + 12)
                        local yaw = readFloat(cam + po + 16)
                        if looksPos(lx, ly, lz) and pitch and yaw and math.abs(pitch) &lt;= 90.5 then
                          st.camLoc = {lx, ly, lz}
                          return pitch, yaw, "cam"
                        end
                      end
                    end
                  end
                end
              end
              return nil
            end

            local function forward(pitch, yaw)
              local pr = math.rad(pitch)
              local yr = math.rad(yaw)
              local cp = math.cos(pr)
              return cp * math.cos(yr), cp * math.sin(yr), math.sin(pr)
            end

            local function teleportToAim()
              local p = pawn()
              if not p then
                print("TP: no pawn")
                return
              end
              local loc = findLocAddr(p)
              if not loc then
                showMessage("Teleport: could not find player XYZ. Move a bit and try again.")
                return
              end
              local pitch, yaw, src = findAim(p)
              if not pitch then
                showMessage("Teleport: could not find aim rotation. Look around and try again.")
                return
              end
              local fx, fy, fz = forward(pitch, yaw)
              local x, y, z = readVec(loc)
              local ox, oy, oz = x, y, z
              -- Prefer camera origin when available so aim matches crosshair
              if st.camLoc then
                ox, oy, oz = st.camLoc[1], st.camLoc[2], st.camLoc[3]
              end
              local nx = ox + fx * st.dist
              local ny = oy + fy * st.dist
              local nz = oz + fz * st.dist
              writeVec(loc, nx, ny, nz)
              -- Also poke CMC location mirrors if present so physics catches up
              local cmc = readQword(p + 0xDC8)
              if cmc and cmc &gt; 0x10000 then
                for _, lo in ipairs({0x2A0, 0x2B0, 0x2C0, 0x2D0, 0x300, 0x310, 0x320}) do
                  local tx, ty, tz = readVec(cmc + lo)
                  if looksPos(tx, ty, tz) then writeVec(cmc + lo, nx, ny, nz) end
                end
              end
              print(string.format(
                "TP aim (%s) pitch=%.1f yaw=%.1f dist=%.0f -&gt; %.1f %.1f %.1f",
                src or "?", pitch, yaw, st.dist, nx, ny, nz
              ))
            end

            -- Discover once on enable
            do
              local p = pawn()
              if p then findLocAddr(p); findAim(p) end
            end

            st.hk = createHotkey(function()
              teleportToAim()
            end, VK_F6)

            print(string.format(
              "Teleport to aim ON — press F6 to blink along crosshair (dist=%.0f uu). Edit gg_grounded_tp.dist in Lua Engine to change range.",
              st.dist
            ))

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_grounded_tp
            if st and st.hk then
              st.hk.destroy()
              st.hk = nil
            end
            print("Teleport to aim OFF")
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>8</ID>
              <Description>"[Script] Set teleport distance…"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            gg_grounded_tp = gg_grounded_tp or {}
            local cur = gg_grounded_tp.dist or 2500
            local n = inputQuery("Teleport distance (Unreal units, ~cm)", "How far along aim to blink?", tostring(cur))
            if n == nil or n == "" then error("cancelled") end
            local v = tonumber(n)
            if not v or v &lt; 100 or v &gt; 200000 then
              showMessage("Use a number between 100 and 200000")
              error("bad dist")
            end
            gg_grounded_tp.dist = v
            print("Teleport distance set to "..tostring(v))
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>9</ID>
              <Description>"Movement Mode (1=walk 5=fly)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>178</Offset>
                <Offset>DC8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>10</ID>
              <Description>"Flying Cheat flag"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>3A8</Offset>
                <Offset>DC8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>11</ID>
              <Description>"Collision byte (0=noclip)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>64</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
                            <CheatEntry>
              <ID>470</ID>
              <Description>"Teleport bookmarks"</Description>
              <Options moHideChildren="1" moManualExpandCollapse="1"/>
              <Color>0040E0D0</Color>
              <GroupHeader>1</GroupHeader>
              <CheatEntries>
                <CheatEntry>
                  <ID>471</ID>
                  <Description>"[Script] Save bookmark A (current pos)"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            gg2_tp_marks = gg2_tp_marks or {}
            local function looksPos(x, y, z)
              if not x or not y or not z then return false end
              if not (x==x and y==y and z==z) then return false end
              local a = math.abs(x)+math.abs(y)
              return a &gt; 500 and a &lt; 500000 and math.abs(z) &lt; 200000
            end
            local function findLoc(p)
              if gg_grounded_tp and gg_grounded_tp.locAddr then
                local x=readFloat(gg_grounded_tp.locAddr); local y=readFloat(gg_grounded_tp.locAddr+4); local z=readFloat(gg_grounded_tp.locAddr+8)
                if looksPos(x,y,z) then return gg_grounded_tp.locAddr, x,y,z end
              end
              local rootOffs = {0x130,0x138,0x140,0x190,0x198,0x1A0,0x1A8}
              local locOffs = {0x11C,0x120,0x128,0x140,0x1C0,0x1D0,0x1E0}
              for _, ro in ipairs(rootOffs) do
                local root = readQword(p + ro)
                if root and root &gt; 0x10000 then
                  for _, lo in ipairs(locOffs) do
                    local x=readFloat(root+lo); local y=readFloat(root+lo+4); local z=readFloat(root+lo+8)
                    if looksPos(x,y,z) then return root+lo, x,y,z end
                  end
                end
              end
              return nil
            end
            local p = readQword(getAddress("Player"))
            local addr,x,y,z = findLoc(p)
            if not addr then showMessage("Could not find player XYZ. Enable Teleport to aim once first."); error("no loc") end
            gg2_tp_marks.A = {x=x,y=y,z=z}
            gg2_tp_marks.locAddr = addr
            print(string.format("Bookmark A saved: %.1f %.1f %.1f", x,y,z))
            showMessage(string.format("Bookmark A saved:\n%.0f  %.0f  %.0f", x,y,z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>472</ID>
                  <Description>"[Script] Save bookmark B (current pos)"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            gg2_tp_marks = gg2_tp_marks or {}
            local function looksPos(x, y, z)
              if not x or not y or not z then return false end
              local a = math.abs(x)+math.abs(y)
              return a &gt; 500 and a &lt; 500000 and math.abs(z) &lt; 200000
            end
            local function findLoc(p)
              if gg2_tp_marks.locAddr then
                local x=readFloat(gg2_tp_marks.locAddr); local y=readFloat(gg2_tp_marks.locAddr+4); local z=readFloat(gg2_tp_marks.locAddr+8)
                if looksPos(x,y,z) then return gg2_tp_marks.locAddr, x,y,z end
              end
              if gg_grounded_tp and gg_grounded_tp.locAddr then
                local x=readFloat(gg_grounded_tp.locAddr); local y=readFloat(gg_grounded_tp.locAddr+4); local z=readFloat(gg_grounded_tp.locAddr+8)
                if looksPos(x,y,z) then return gg_grounded_tp.locAddr, x,y,z end
              end
              local p2 = p
              local rootOffs = {0x130,0x138,0x140,0x190,0x198,0x1A0}
              local locOffs = {0x11C,0x120,0x128,0x140,0x1C0,0x1D0}
              for _, ro in ipairs(rootOffs) do
                local root = readQword(p2 + ro)
                if root and root &gt; 0x10000 then
                  for _, lo in ipairs(locOffs) do
                    local x=readFloat(root+lo); local y=readFloat(root+lo+4); local z=readFloat(root+lo+8)
                    if looksPos(x,y,z) then return root+lo, x,y,z end
                  end
                end
              end
              return nil
            end
            local p = readQword(getAddress("Player"))
            local addr,x,y,z = findLoc(p)
            if not addr then error("no loc") end
            gg2_tp_marks.B = {x=x,y=y,z=z}
            gg2_tp_marks.locAddr = addr
            print(string.format("Bookmark B saved: %.1f %.1f %.1f", x,y,z))
            showMessage(string.format("Bookmark B saved:\n%.0f  %.0f  %.0f", x,y,z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>473</ID>
                  <Description>"[Script] Save bookmark C (current pos)"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            gg2_tp_marks = gg2_tp_marks or {}
            local function looksPos(x, y, z)
              if not x or not y or not z then return false end
              local a = math.abs(x)+math.abs(y)
              return a &gt; 500 and a &lt; 500000 and math.abs(z) &lt; 200000
            end
            local p = readQword(getAddress("Player"))
            local addr = gg2_tp_marks.locAddr or (gg_grounded_tp and gg_grounded_tp.locAddr)
            local x,y,z
            if addr then x=readFloat(addr); y=readFloat(addr+4); z=readFloat(addr+8) end
            if not looksPos(x,y,z) then
              showMessage("Save bookmark A first (or enable Teleport to aim once)."); error("no loc")
            end
            gg2_tp_marks.C = {x=x,y=y,z=z}
            print(string.format("Bookmark C saved: %.1f %.1f %.1f", x,y,z))
            showMessage(string.format("Bookmark C saved:\n%.0f  %.0f  %.0f", x,y,z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>474</ID>
                  <Description>"[Script] Warp to bookmark A"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            gg2_tp_marks = gg2_tp_marks or {}
            local m = gg2_tp_marks.A
            if not m then showMessage("Save bookmark A first"); error("no A") end
            local addr = gg2_tp_marks.locAddr or (gg_grounded_tp and gg_grounded_tp.locAddr)
            if not addr then showMessage("No location pointer. Enable Teleport to aim once."); error("no addr") end
            writeFloat(addr, m.x); writeFloat(addr+4, m.y); writeFloat(addr+8, m.z)
            print(string.format("Warped to A: %.1f %.1f %.1f", m.x,m.y,m.z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>475</ID>
                  <Description>"[Script] Warp to bookmark B"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            gg2_tp_marks = gg2_tp_marks or {}
            local m = gg2_tp_marks.B
            if not m then showMessage("Save bookmark B first"); error("no B") end
            local addr = gg2_tp_marks.locAddr or (gg_grounded_tp and gg_grounded_tp.locAddr)
            if not addr then error("no addr") end
            writeFloat(addr, m.x); writeFloat(addr+4, m.y); writeFloat(addr+8, m.z)
            print(string.format("Warped to B: %.1f %.1f %.1f", m.x,m.y,m.z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>476</ID>
                  <Description>"[Script] Warp to bookmark C"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            gg2_tp_marks = gg2_tp_marks or {}
            local m = gg2_tp_marks.C
            if not m then showMessage("Save bookmark C first"); error("no C") end
            local addr = gg2_tp_marks.locAddr or (gg_grounded_tp and gg_grounded_tp.locAddr)
            if not addr then error("no addr") end
            writeFloat(addr, m.x); writeFloat(addr+4, m.y); writeFloat(addr+8, m.z)
            print(string.format("Warped to C: %.1f %.1f %.1f", m.x,m.y,m.z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>477</ID>
                  <Description>"[Script] Save death / last pos snapshot"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            -- Continuously snapshot position every 2s into Last so you can warp back after a fall/death scramble
            if getAddressSafe("Player") == nil then error("no Player") end
            gg2_tp_marks = gg2_tp_marks or {}
            gg2_tp_last = gg2_tp_last or {}
            local st = gg2_tp_last
            st.timer = createTimer(nil, false)
            st.timer.Interval = 2000
            st.timer.OnTimer = function()
              local addr = gg2_tp_marks.locAddr or (gg_grounded_tp and gg_grounded_tp.locAddr)
              if not addr then return end
              local x=readFloat(addr); local y=readFloat(addr+4); local z=readFloat(addr+8)
              if x and y and z and (math.abs(x)+math.abs(y)) &gt; 500 then
                gg2_tp_marks.Last = {x=x,y=y,z=z}
              end
            end
            st.timer.Enabled = true
            print("Last-pos snapshot ON (every 2s). Warp with Warp to last snapshot.")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_tp_last
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            </AssemblerScript>
                </CheatEntry>
                <CheatEntry>
                  <ID>478</ID>
                  <Description>"[Script] Warp to last snapshot"</Description>
                  <Color>0000FF00</Color>
                  <VariableType>Auto Assembler Script</VariableType>
                  <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            gg2_tp_marks = gg2_tp_marks or {}
            local m = gg2_tp_marks.Last
            if not m then showMessage("Enable Save death / last pos snapshot first, walk around, then warp."); error("no Last") end
            local addr = gg2_tp_marks.locAddr or (gg_grounded_tp and gg_grounded_tp.locAddr)
            if not addr then error("no addr") end
            writeFloat(addr, m.x); writeFloat(addr+4, m.y); writeFloat(addr+8, m.z)
            print(string.format("Warped to Last: %.1f %.1f %.1f", m.x,m.y,m.z))
            [DISABLE]
            </AssemblerScript>
                </CheatEntry>
              </CheatEntries>
            </CheatEntry>
        <CheatEntry>
          <ID>400</ID>
          <Description>"Buggy (mount)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>008FBF4A</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>401</ID>
              <Description>"README — mount while in-world"</Description>
              <Color>00808080</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>{
        Buggy live cheats (Grounded 2 / Augusta)
        ========================================
        1. Enable [ACTIVATE]
        2. Mount your buggy
        3. Run [Script] Re-probe CMC (mounted) once
        4. Enable speed / stamina / god / noclip as needed

        Mount may keep Player on the teen OR possess the buggy pawn.
        Scripts dual-write CMC on Player+gg2_cmc_off and any extra
        CMC candidates found by the mounted re-probe (gg2_buggy_cmcs).

        Offline buggy TIER stays in the save editor.
        }
        [ENABLE]
        [DISABLE]
        </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>402</ID>
              <Description>"[Script] Re-probe CMC (mounted / wide)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local p = readQword(getAddress("Player"))
            if not p or p == 0 then error("null Player") end
            local pawns = {0xDC8, 0xE00, 0xE08, 0xD80, 0xD00, 0xC80, 0xF00, 0x1080, 0x1100, 0xB80, 0xC00, 0x1180, 0x1200}
            gg2_buggy_cmcs = {}
            local bestOff, bestScore = nil, -1
            print(string.format("Buggy CMC probe on pawn %X …", p))
            for _, off in ipairs(pawns) do
              local c = readQword(p + off)
              if c and c &gt; 0x10000 and c &lt; 0x7FFFFFFFFFFF then
                for foff = 0x100, 0x480, 4 do
                  local v = readFloat(c + foff)
                  if v and v &gt; 80 and v &lt; 4000 then
                    local score = 0
                    if v &gt; 150 and v &lt; 900 then score = score + 3 end
                    if v &gt; 900 and v &lt; 2500 then score = score + 2 end
                    score = score + 1
                    print(string.format("  Player+0x%X CMC=%X walk~%.0f @ +0x%X", off, c, v, foff))
                    local entry = {pawnOff = off, cmc = c, walkOff = foff, walk = v}
                    table.insert(gg2_buggy_cmcs, entry)
                    if score &gt; bestScore then
                      bestScore = score
                      bestOff = off
                      gg2_cmc_off = off
                    end
                    break
                  end
                end
              end
            end
            -- Dedupe by CMC address
            local seen, uniq = {}, {}
            for _, e in ipairs(gg2_buggy_cmcs) do
              if e.cmc and not seen[e.cmc] then
                seen[e.cmc] = true
                table.insert(uniq, e)
              end
            end
            gg2_buggy_cmcs = uniq
            if bestOff then
              print(string.format("Primary CMC Player+0x%X (%d candidate(s))", bestOff, #gg2_buggy_cmcs))
              showMessage(string.format("Mounted CMC probe OK.\nPrimary Player+0x%X\n%d CMC candidate(s) cached.\nNow enable Buggy Super Speed / God / etc.", bestOff, #gg2_buggy_cmcs))
            else
              gg2_cmc_off = gg2_cmc_off or 0xDC8
              showMessage("No CMC-like floats found. Move/sprint on the buggy, then re-run this probe.")
            end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>403</ID>
              <Description>"[Script] Buggy Super Speed x5"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            gg2_buggy_speed = gg2_buggy_speed or {}
            local st = gg2_buggy_speed
            st.mult = 5.0
            st.targets = {}

            local function findWalk(c)
              local prefs = {600, 500, 450, 400, 350, 300, 250, 700, 800, 1000, 1200}
              local bestOff, bestScore, base = nil, 999, 600
              for off = 0x100, 0x480, 4 do
                local v = readFloat(c + off)
                if v and v &gt; 60 and v &lt; 5000 then
                  for _, t in ipairs(prefs) do
                    local d = math.abs(v - t)
                    if d &lt; bestScore then
                      bestScore = d
                      bestOff = off
                      base = (d &lt; 40) and t or v
                    end
                  end
                end
              end
              return bestOff, base
            end

            local function collectCmcs()
              local list = {}
              local p = readQword(getAddress("Player"))
              if not p or p == 0 then return list end
              local primary = readQword(p + (gg2_cmc_off or 0xDC8))
              if primary and primary &gt; 0x10000 then table.insert(list, primary) end
              if gg2_buggy_cmcs then
                for _, e in ipairs(gg2_buggy_cmcs) do
                  local c = e.cmc
                  if (not c) and e.pawnOff then c = readQword(p + e.pawnOff) end
                  if c and c &gt; 0x10000 then table.insert(list, c) end
                end
              end
              -- also probe a few common slots live
              for _, off in ipairs({0xDC8, 0xE00, 0xE08, 0xD80, 0xF00}) do
                local c = readQword(p + off)
                if c and c &gt; 0x10000 then table.insert(list, c) end
              end
              local seen, out = {}, {}
              for _, c in ipairs(list) do
                if not seen[c] then seen[c] = true; table.insert(out, c) end
              end
              return out
            end

            for _, c in ipairs(collectCmcs()) do
              local off, base = findWalk(c)
              if off then
                table.insert(st.targets, {cmc = c, off = off, base = base})
                print(string.format("Buggy speed target CMC %X +0x%X base~%.0f", c, off, base))
              end
            end
            if #st.targets == 0 then
              showMessage("No walk-speed floats found. Mount the buggy, run Re-probe CMC, sprint once, then re-enable.")
              error("no buggy walk")
            end

            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              local p = readQword(getAddressSafe("Player") or 0)
              for _, t in ipairs(st.targets) do
                local c = t.cmc
                -- refresh CMC from pawn if possible
                if p and p ~= 0 and gg2_cmc_off then
                  local live = readQword(p + (gg2_cmc_off or 0xDC8))
                  if live and live &gt; 0x10000 then c = live end
                end
                if c and t.off then
                  writeFloat(c + t.off, (t.base or 600) * st.mult)
                  for _, d in ipairs({-8, -4, 4, 8, 12, 16, 20}) do
                    local v = readFloat(c + t.off + d)
                    if v and v &gt; 40 and v &lt; 5000 then
                      writeFloat(c + t.off + d, math.max(v, (t.base or 600) * st.mult * 0.55))
                    end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print(string.format("Buggy Super Speed x%.1f on %d CMC(s)", st.mult, #st.targets))

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_buggy_speed
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            if st and st.targets then
              for _, t in ipairs(st.targets) do
                if t.cmc and t.off and t.base then
                  pcall(function() writeFloat(t.cmc + t.off, t.base) end)
                end
              end
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>404</ID>
              <Description>"[Script] Infinite stamina (rider + drain off)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_buggy_stam = gg2_buggy_stam or {}
            local st = gg2_buggy_stam
            -- Snapshot GameState stamina drain byte if present
            local drain = resolve("GameState", {0x40, 0x138, 0x388, 0x170})
            if drain then
              st.prevDrain = readBytes(drain, 1, true)
              writeBytes(drain, 0)
              print("Stamina Drain byte -&gt; 0")
            else
              print("Stamina Drain pointer miss (ok if GameState AOB weak)")
            end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 80
            st.timer.OnTimer = function()
              local cur = resolve("Player", {0xD8, 0x150, 0x1B8})
              local base = resolve("Player", {0xDC, 0x150, 0x1B8})
              if cur then writeFloat(cur, 200) end
              if base then writeFloat(base, 200) end
              -- also poke any probe-like stamina floats cached from re-probe vitals
              if gg2_buggy_stam_addrs then
                for _, a in ipairs(gg2_buggy_stam_addrs) do
                  pcall(function() writeFloat(a, 200) end)
                end
              end
              local d = resolve("GameState", {0x40, 0x138, 0x388, 0x170})
              if d then writeBytes(d, 0) end
            end
            st.timer.Enabled = true
            print("Infinite stamina ON (rider chain + drain off)")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_buggy_stam
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local drain = resolve("GameState", {0x40, 0x138, 0x388, 0x170})
            if drain and st and st.prevDrain ~= nil then
              writeBytes(drain, st.prevDrain)
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>405</ID>
              <Description>"[Script] Buggy / rider god HP"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            -- Collect HP-like floats on pawn components (works if Player is teen OR buggy)
            local function probeHpAddrs(p)
              local addrs = {}
              local pet = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
              if pet then
                writeBytes(pet, 1)
                print("Pet Invincibility byte -&gt; 1")
              end
              for off = 0x100, 0x1400, 8 do
                local c = readQword(p + off)
                if c and c &gt; 0x10000 and c &lt; 0x7FFFFFFFFFFF then
                  for f = 0, 0xA0, 4 do
                    local v = readFloat(c + f)
                    if v and v &gt; 30 and v &lt; 800 then
                      -- prefer damaged=0 style siblings: if nearby float is small, treat this as base HP
                      table.insert(addrs, {base = c + f, obj = c, foff = f})
                      if #addrs &gt; 12 then return addrs end
                      break
                    end
                  end
                end
              end
              return addrs
            end

            gg2_buggy_god = gg2_buggy_god or {}
            local st = gg2_buggy_god
            local p = readQword(getAddress("Player"))
            st.probe = probeHpAddrs(p)
            st.petPrev = nil
            local pet = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
            if pet then st.petPrev = readBytes(pet, 1, true) end

            st.timer = createTimer(nil, false)
            st.timer.Interval = 100
            st.timer.OnTimer = function()
              local dmg = resolve("Player", {0x284, 0x30, 0x1B8})
              local base = resolve("Player", {0x280, 0x30, 0x1B8})
              if dmg then writeFloat(dmg, 0) end
              if base then writeFloat(base, 200) end
              local pet2 = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
              if pet2 then writeBytes(pet2, 1) end
              -- Freeze probed HP-ish floats toward a healthy base
              if st.probe then
                for _, h in ipairs(st.probe) do
                  local v = readFloat(h.base)
                  if v and v &lt; 50 then
                    writeFloat(h.base, 200)
                  elseif v and v &gt; 50 and v &lt; 800 then
                    -- keep current max-ish; heal damage companions near 0
                    for _, d in ipairs({-8, -4, 4, 8}) do
                      local n = readFloat(h.base + d)
                      if n and n &gt;= 0 and n &lt; 5 then writeFloat(h.base + d, 0) end
                    end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print(string.format("Buggy/rider god HP ON (%d probe hits + G1 health chain + pet invuln)", #(st.probe or {})))

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_buggy_god
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local pet = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
            if pet and st and st.petPrev ~= nil then writeBytes(pet, st.petPrev) end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>406</ID>
              <Description>"[Script] Buggy no-clip fly"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local function cmcOff()
              return gg2_cmc_off or 0xDC8
            end
            gg2_buggy_noclip = gg2_buggy_noclip or {}
            local st = gg2_buggy_noclip
            do
              local col = resolve("Player", {0x64})
              if col then st.prevCollision = readBytes(col, 1, true) end
            end
            local function apply()
              local off = cmcOff()
              local fly = resolve("Player", {0x3A8, off})
              local mode = resolve("Player", {0x178, off})
              local col = resolve("Player", {0x64})
              if fly then writeByte(fly, 1) end
              if mode then writeByte(mode, 5) end
              if col then writeByte(col, 0) end
              local p = readQword(getAddress("Player"))
              if not p or p == 0 then return end
              local cmcs = {}
              table.insert(cmcs, readQword(p + off))
              if gg2_buggy_cmcs then
                for _, e in ipairs(gg2_buggy_cmcs) do
                  if e.pawnOff then table.insert(cmcs, readQword(p + e.pawnOff)) end
                  if e.cmc then table.insert(cmcs, e.cmc) end
                end
              end
              for _, c in ipairs(cmcs) do
                if c and c &gt; 0x10000 then
                  local fly2 = c + 0x3A8
                  local mode2 = c + 0x178
                  pcall(function() writeByte(fly2, 1) end)
                  pcall(function() writeByte(mode2, 5) end)
                  for _, goff in ipairs({0x170, 0x174, 0x178, 0x17C, 0x180}) do
                    local v = readFloat(c + goff)
                    if v and v &gt; 0.2 and v &lt; 5.0 then writeFloat(c + goff, 0.0) end
                  end
                end
              end
            end
            apply()
            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function() apply() end
            st.timer.Enabled = true
            print("Buggy no-clip fly ON")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local st = gg2_buggy_noclip
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            local off = gg2_cmc_off or 0xDC8
            local fly = resolve("Player", {0x3A8, off})
            local mode = resolve("Player", {0x178, off})
            local col = resolve("Player", {0x64})
            if fly then writeByte(fly, 0) end
            if mode then writeByte(mode, 1) end
            if col then
              if st and st.prevCollision ~= nil then writeByte(col, st.prevCollision)
              else writeByte(col, 1) end
            end
            print("Buggy no-clip fly OFF")
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>407</ID>
              <Description>"[Script] Buggy combo (speed + stam + god)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE], mount buggy, optionally Re-probe CMC, then enable combo.")
              error("no Player")
            end
            -- Kick the three sibling scripts by synthesizing their enable logic lightly:
            -- 1) widen CMC cache if empty
            if not gg2_buggy_cmcs or #gg2_buggy_cmcs == 0 then
              print("Combo: run Re-probe CMC (mounted) first for best results � continuing with Player CMC")
            end
            -- 2) start speed if not already
            if not (gg2_buggy_speed and gg2_buggy_speed.timer) then
              print("Combo: enable [Script] Buggy Super Speed x5 for speed (tick it).")
            end
            if not (gg2_buggy_stam and gg2_buggy_stam.timer) then
              print("Combo: enable [Script] Infinite stamina (tick it).")
            end
            if not (gg2_buggy_god and gg2_buggy_god.timer) then
              print("Combo: enable [Script] Buggy / rider god HP (tick it).")
            end
            -- Also force pet invuln + stamina drain off immediately
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_buggy_combo = gg2_buggy_combo or {}
            local st = gg2_buggy_combo
            local pet = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
            local drain = resolve("GameState", {0x40, 0x138, 0x388, 0x170})
            if pet then st.petPrev = readBytes(pet, 1, true); writeBytes(pet, 1) end
            if drain then st.drainPrev = readBytes(drain, 1, true); writeBytes(drain, 0) end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 80
            st.timer.OnTimer = function()
              local dmg = resolve("Player", {0x284, 0x30, 0x1B8})
              local base = resolve("Player", {0x280, 0x30, 0x1B8})
              local stam = resolve("Player", {0xD8, 0x150, 0x1B8})
              if dmg then writeFloat(dmg, 0) end
              if base then writeFloat(base, 200) end
              if stam then writeFloat(stam, 200) end
              local p2 = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
              local d2 = resolve("GameState", {0x40, 0x138, 0x388, 0x170})
              if p2 then writeBytes(p2, 1) end
              if d2 then writeBytes(d2, 0) end
              -- speed nudge on primary CMC
              local p = readQword(getAddressSafe("Player") or 0)
              if p and p ~= 0 then
                local c = readQword(p + (gg2_cmc_off or 0xDC8))
                if c and c &gt; 0x10000 then
                  for off = 0x140, 0x360, 4 do
                    local v = readFloat(c + off)
                    if v and v &gt; 80 and v &lt; 2500 then
                      writeFloat(c + off, math.max(v, 600 * 5))
                      break
                    end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print("Buggy combo ON (god HP + stam + pet invuln + drain off + speed nudge). Also tick Super Speed / God scripts for dual-CMC.")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_buggy_combo
            if st and st.timer then st.timer.Enabled = false; st.timer.destroy(); st.timer = nil end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local pet = resolve("GameState", {0x47, 0x138, 0x388, 0x170})
            local drain = resolve("GameState", {0x40, 0x138, 0x388, 0x170})
            if pet and st and st.petPrev ~= nil then writeBytes(pet, st.petPrev) end
            if drain and st and st.drainPrev ~= nil then writeBytes(drain, st.drainPrev) end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>408</ID>
              <Description>"Pet Invincibility (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>47</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>409</ID>
              <Description>"Stamina Drain (0=off, manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>40</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>33</ID>
          <Description>"Vitals"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>004080FF</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>13</ID>
              <Description>"[Script] Fill vitals once"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function wchain(baseSym, offsets, isFloat, value)
              local a = getAddress(baseSym)
              for i = #offsets, 1, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return false end
              end
              -- last offset is field
              -- rebuild: CE order is final field first
              return true
            end

            -- Helper: resolve CE-style offsets (list is top=final field ... bottom=first deref)
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            local writes = {
              {desc="health damaged", off={0x284,0x30,0x1B8}, v=0, float=true},
              {desc="base health", off={0x280,0x30,0x1B8}, v=200, float=true},
              {desc="stamina", off={0xD8,0x150,0x1B8}, v=200, float=true},
              {desc="hunger", off={0x278,0x130,0x1B8}, v=5, float=true},
              {desc="thirst", off={0x27C,0x130,0x1B8}, v=5, float=true},
              {desc="oxygen", off={0x280,0x130,0x1B8}, v=100, float=true},
            }
            for _, w in ipairs(writes) do
              local addr = resolve("Player", w.off)
              if addr then
                if w.float then writeFloat(addr, w.v) else writeInteger(addr, w.v) end
                print("set "..w.desc)
              else
                print("miss "..w.desc)
              end
            end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>14</ID>
              <Description>"[Script] God mode (freeze HP/stamina)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg_grounded_god = gg_grounded_god or {}
            local st = gg_grounded_god
            st.timer = createTimer(nil, false)
            st.timer.Interval = 100
            st.timer.OnTimer = function()
              local dmg = resolve("Player", {0x284,0x30,0x1B8})
              local base = resolve("Player", {0x280,0x30,0x1B8})
              local stam = resolve("Player", {0xD8,0x150,0x1B8})
              if dmg then writeFloat(dmg, 0) end
              if base then writeFloat(base, 200) end
              if stam then writeFloat(stam, 200) end
            end
            st.timer.Enabled = true
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_grounded_god
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>15</ID>
              <Description>"[Script] Infinite hunger/thirst/oxygen"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg_grounded_surv = gg_grounded_surv or {}
            local st = gg_grounded_surv
            st.timer = createTimer(nil, false)
            st.timer.Interval = 250
            st.timer.OnTimer = function()
              local hunger = resolve("Player", {0x278,0x130,0x1B8})
              local thirst = resolve("Player", {0x27C,0x130,0x1B8})
              local oxygen = resolve("Player", {0x280,0x130,0x1B8})
              if hunger then writeFloat(hunger, 5) end
              if thirst then writeFloat(thirst, 5) end
              if oxygen then writeFloat(oxygen, 100) end
            end
            st.timer.Enabled = true
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_grounded_surv
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>18</ID>
              <Description>"Health"</Description>
              <Options moHideChildren="1" moManualExpandCollapse="1"/>
              <Color>00FF8080</Color>
              <GroupHeader>1</GroupHeader>
              <CheatEntries>
                <CheatEntry>
                  <ID>16</ID>
                  <Description>"Health Damaged (0=full)"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>284</Offset>
                    <Offset>30</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>17</ID>
                  <Description>"Base Health"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>280</Offset>
                    <Offset>30</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
              </CheatEntries>
            </CheatEntry>
            <CheatEntry>
              <ID>23</ID>
              <Description>"Stamina"</Description>
              <Options moHideChildren="1" moManualExpandCollapse="1"/>
              <Color>00FF8080</Color>
              <GroupHeader>1</GroupHeader>
              <CheatEntries>
                <CheatEntry>
                  <ID>19</ID>
                  <Description>"Current Stamina"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>D8</Offset>
                    <Offset>150</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>20</ID>
                  <Description>"Base Stamina"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>DC</Offset>
                    <Offset>150</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>21</ID>
                  <Description>"Regen Delay"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>E0</Offset>
                    <Offset>150</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>22</ID>
                  <Description>"Regen Rate"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>E4</Offset>
                    <Offset>150</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
              </CheatEntries>
            </CheatEntry>
            <CheatEntry>
              <ID>32</ID>
              <Description>"Survival"</Description>
              <Options moHideChildren="1" moManualExpandCollapse="1"/>
              <Color>00FF8080</Color>
              <GroupHeader>1</GroupHeader>
              <CheatEntries>
                <CheatEntry>
                  <ID>24</ID>
                  <Description>"Hunger"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>278</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>25</ID>
                  <Description>"Thirst"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>27C</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>26</ID>
                  <Description>"Oxygen"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>280</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>27</ID>
                  <Description>"Max Hunger"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>138</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>28</ID>
                  <Description>"Max Thirst"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>180</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>29</ID>
                  <Description>"Max Oxygen"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>1C8</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>30</ID>
                  <Description>"Oxygen Rate"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>1CC</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
                <CheatEntry>
                  <ID>31</ID>
                  <Description>"Decay Rate"</Description>
                  <ShowAsSigned>0</ShowAsSigned>
                  <VariableType>Float</VariableType>
                  <Address>Player</Address>
                  <Offsets>
                    <Offset>284</Offset>
                    <Offset>130</Offset>
                    <Offset>1B8</Offset>
                  </Offsets>
                </CheatEntry>
              </CheatEntries>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>40</ID>
          <Description>"Economy (science / molars)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>0040C080</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>34</ID>
              <Description>"[Script] Max science + molars"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Statistics") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Statistics")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local science = resolve("Statistics", {0x470,0x2D0,0x170,0xB0,0x20})
            local milk = resolve("Statistics", {0x480,0x2D0,0x170,0xB0,0x20})
            local mega = resolve("Statistics", {0x47C,0x2D0,0x170,0xB0,0x20})
            if science then writeInteger(science, 999999) end
            if milk then writeInteger(milk, 999) end
            if mega then writeInteger(mega, 999) end
            print("Science/molars topped up")
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>35</ID>
              <Description>"Raw Science"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Statistics</Address>
              <Offsets>
                <Offset>470</Offset>
                <Offset>2D0</Offset>
                <Offset>170</Offset>
                <Offset>B0</Offset>
                <Offset>20</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>36</ID>
              <Description>"Milk Molars"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Statistics</Address>
              <Offsets>
                <Offset>480</Offset>
                <Offset>2D0</Offset>
                <Offset>170</Offset>
                <Offset>B0</Offset>
                <Offset>20</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>37</ID>
              <Description>"Mega Milk Molars"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Statistics</Address>
              <Offsets>
                <Offset>47C</Offset>
                <Offset>2D0</Offset>
                <Offset>170</Offset>
                <Offset>B0</Offset>
                <Offset>20</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>38</ID>
              <Description>"Brainpower"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Statistics</Address>
              <Offsets>
                <Offset>474</Offset>
                <Offset>2D0</Offset>
                <Offset>170</Offset>
                <Offset>B0</Offset>
                <Offset>20</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>39</ID>
              <Description>"Level"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Statistics</Address>
              <Offsets>
                <Offset>478</Offset>
                <Offset>2D0</Offset>
                <Offset>170</Offset>
                <Offset>B0</Offset>
                <Offset>20</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>45</ID>
          <Description>"Mutations / hauling"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00C08040</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>41</ID>
              <Description>"Max Mutations"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>100</Offset>
                <Offset>8</Offset>
                <Offset>1A8</Offset>
                <Offset>258</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>42</ID>
              <Description>"Active Mutations"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>104</Offset>
                <Offset>8</Offset>
                <Offset>1A8</Offset>
                <Offset>258</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>43</ID>
              <Description>"Haul Capacity"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>E0</Offset>
                <Offset>198</Offset>
                <Offset>18</Offset>
                <Offset>1A8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>44</ID>
              <Description>"Haul"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>120</Offset>
                <Offset>198</Offset>
                <Offset>18</Offset>
                <Offset>1A8</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>51</ID>
          <Description>"Held gear (right hand)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>008080C0</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>46</ID>
              <Description>"Stack"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>140</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>47</ID>
              <Description>"Durability"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>228</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>48</ID>
              <Description>"Base Enhancement"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>250</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>49</ID>
              <Description>"Bonus Enhancement"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>254</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>50</ID>
              <Description>"Spoil Time"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>158</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        
        
        <CheatEntry>
          <ID>320</ID>
          <Description>"Stacks — giant stack size"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>0040C0A0</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>321</ID>
              <Description>"[Script] Giant held stack (freeze 9999)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            local function equipRoot()
              local p = readQword(getAddress("Player"))
              if not p or p == 0 then return nil end
              for _, eoff in ipairs({0x16E8, 0x16F0, 0x1700, 0x16E0, 0x1600}) do
                local r = readQword(p + eoff)
                if r and r &gt; 0x10000 then return r end
              end
              return nil
            end

            -- Stack field on held item is commonly +0x140 (same as CT held Stack pointer).
            local STACK_OFFS = {0x140, 0x138, 0x148, 0x130, 0x150, 0x13C, 0x144}

            gg_giant_held = gg_giant_held or {}
            local st = gg_giant_held
            st.qty = 9999
            st.timer = createTimer(nil, false)
            st.timer.Interval = 150
            st.timer.OnTimer = function()
              local addr = resolve("Player", {0x140, 0x200, 0x16E8})
              if addr then
                local cur = readInteger(addr)
                if cur and cur &gt;= 0 and cur &lt; 100000 then
                  writeInteger(addr, st.qty)
                end
              end
              local root = equipRoot()
              if not root then return end
              for _, soff in ipairs({0x200, 0x1F8, 0x208, 0x1F0, 0x210}) do
                local item = readQword(root + soff)
                if item and item &gt; 0x10000 then
                  for _, foff in ipairs(STACK_OFFS) do
                    local v = readInteger(item + foff)
                    if v and v &gt;= 1 and v &lt; 50000 then
                      writeInteger(item + foff, st.qty)
                    end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print("Giant held stack: freezing quantity at "..tostring(st.qty))

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_giant_held
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>322</ID>
              <Description>"[Script] Giant StackSize upgrades (tier 20)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe(process) == nil then
              showMessage("Attach to the game first")
              error("no process")
            end

            -- Mirror save-editor giant stacks: push StackSize.* upgrade levels to 20.
            -- Scan writable memory for ASCII names, then write nearby i32 levels.
            local needles = {
              {hex="53 74 61 63 6B 53 69 7A 65 2E 46 6F 6F 64", name="StackSize.Food", len=14},
              {hex="53 74 61 63 6B 53 69 7A 65 2E 52 65 73 6F 75 72 63 65", name="StackSize.Resource", len=18},
              {hex="53 74 61 63 6B 53 69 7A 65 2E 41 6D 6D 6F", name="StackSize.Ammo", len=14},
            }
            local TIER = 20
            local wrote = 0

            local function tryWriteLevels(base, nameLen)
              -- Save layout: FString then i32 level, i32 unk. Live may vary — probe a few spots.
              local candidates = {
                base + nameLen + 1,
                base + nameLen,
                base + nameLen + 4,
                base - 4,
                base + nameLen + 8,
              }
              for _, addr in ipairs(candidates) do
                local v = readInteger(addr)
                if v and v &gt;= 0 and v &lt;= 99 then
                  writeInteger(addr, TIER)
                  wrote = wrote + 1
                  print(string.format("  level @ %X  %d -&gt; %d", addr, v, TIER))
                  return true
                end
              end
              return false
            end

            for _, n in ipairs(needles) do
              local aob = AOBScan(n.hex, "+W*X*C")
              if aob and aob.Count &gt; 0 then
                print(n.name..": "..tostring(aob.Count).." hit(s)")
                local limit = math.min(aob.Count - 1, 12)
                for i = 0, limit do
                  local at = getAddressSafe(aob[i])
                  if at then tryWriteLevels(at, n.len) end
                end
              else
                print(n.name..": MISS (upgrade may use FName only — use held-stack script / save editor)")
              end
              if aob then aob.destroy() end
            end

            -- Also bump haul capacity if the known pointer resolves
            if getAddressSafe("Player") then
              local function resolve(baseSym, offsets)
                local a = getAddress(baseSym)
                if not a then return nil end
                for i = #offsets, 2, -1 do
                  a = readQword(a + offsets[i])
                  if a == nil or a == 0 then return nil end
                end
                return a + offsets[1]
              end
              local haul = resolve("Player", {0xE0, 0x198, 0x18, 0x1A8})
              if haul then
                local prev = readInteger(haul)
                writeInteger(haul, 99)
                print(string.format("Haul capacity %s -&gt; 99", tostring(prev)))
                wrote = wrote + 1
              end
            end

            if wrote == 0 then
              showMessage("No StackSize upgrade levels found in RAM.\nUse Giant held stack, or set stacks in the save editor.")
            else
              print("Giant StackSize: "..tostring(wrote).." write(s). Re-open bags / drop-pickup if UI is stale.")
            end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>323</ID>
              <Description>"[Script] Giant stacks combo (held freeze + upgrades)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            -- One-shot upgrade write
            local needles = {
              {hex="53 74 61 63 6B 53 69 7A 65 2E 46 6F 6F 64", len=14},
              {hex="53 74 61 63 6B 53 69 7A 65 2E 52 65 73 6F 75 72 63 65", len=18},
              {hex="53 74 61 63 6B 53 69 7A 65 2E 41 6D 6D 6F", len=14},
            }
            for _, n in ipairs(needles) do
              local aob = AOBScan(n.hex, "+W*X*C")
              if aob and aob.Count &gt; 0 then
                local limit = math.min(aob.Count - 1, 8)
                for i = 0, limit do
                  local at = getAddressSafe(aob[i])
                  if at then
                    for _, rel in ipairs({n.len + 1, n.len, n.len + 4}) do
                      local v = readInteger(at + rel)
                      if v and v &gt;= 0 and v &lt;= 99 then writeInteger(at + rel, 20) end
                    end
                  end
                end
              end
              if aob then aob.destroy() end
            end

            local haul = resolve("Player", {0xE0, 0x198, 0x18, 0x1A8})
            if haul then writeInteger(haul, 99) end

            gg_giant_combo = gg_giant_combo or {}
            local st = gg_giant_combo
            st.timer = createTimer(nil, false)
            st.timer.Interval = 150
            st.timer.OnTimer = function()
              local addr = resolve("Player", {0x140, 0x200, 0x16E8})
              if addr then
                local cur = readInteger(addr)
                if cur and cur &gt;= 0 and cur &lt; 100000 then writeInteger(addr, 9999) end
              end
              if haul then writeInteger(haul, 99) end
            end
            st.timer.Enabled = true
            print("Giant stacks combo: upgrades→20, haul→99, held stack freeze 9999")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_giant_combo
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>324</ID>
              <Description>"Held Stack (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>140</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>325</ID>
              <Description>"Haul Capacity (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>E0</Offset>
                <Offset>198</Offset>
                <Offset>18</Offset>
                <Offset>1A8</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>

        <CheatEntry>
          <ID>300</ID>
          <Description>"Gear — one-shot / god armor"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00FF6040</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>301</ID>
              <Description>"[Script] One-shot weapons (held + damage x100)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            local function equipRoot()
              local p = readQword(getAddress("Player"))
              if not p or p == 0 then return nil end
              return readQword(p + 0x16E8)
            end

            -- Item instance slots under Equipment (right hand is commonly +0x200).
            local SLOT_OFFS = {0x200, 0x1F8, 0x208, 0x1F0, 0x210, 0x1E8, 0x218, 0x1E0, 0x220, 0x1D8, 0x228, 0x1D0}

            local function buffItem(item)
              if not item or item == 0 or item &lt; 0x10000 then return false end
              -- Durability float (held-gear chain field +0x228)
              local dur = readFloat(item + 0x228)
              if dur and dur &gt;= 0 and dur &lt; 1e8 then
                writeFloat(item + 0x228, 99999)
              end
              -- Base / bonus enhancement ints
              local be = readInteger(item + 0x250)
              local bo = readInteger(item + 0x254)
              if be and be &gt;= 0 and be &lt; 64 then writeInteger(item + 0x250, 15) end
              if bo and bo &gt;= 0 and bo &lt; 64 then writeInteger(item + 0x254, 15) end
              -- Attack-ish floats near enhancement (best-effort)
              for _, foff in ipairs({0x240, 0x244, 0x248, 0x24C, 0x258, 0x25C, 0x260, 0x264}) do
                local v = readFloat(item + foff)
                if v and v &gt; 0.5 and v &lt; 500 then
                  writeFloat(item + foff, 100)
                end
              end
              return true
            end

            gg_gear_oneshot = gg_gear_oneshot or {}
            local st = gg_gear_oneshot
            st.dmgAddr = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
            if st.dmgAddr then
              st.dmgPrev = readFloat(st.dmgAddr)
              writeFloat(st.dmgAddr, 100)
              print(string.format("PlayerDamage scale -&gt; 100 (was %.2f)", st.dmgPrev or -1))
            else
              print("GameState damage scale miss — still buffing held item stats")
            end

            st.timer = createTimer(nil, false)
            st.timer.Interval = 100
            st.timer.OnTimer = function()
              if st.dmgAddr then writeFloat(st.dmgAddr, 100) end
              local root = equipRoot()
              if not root then return end
              for _, soff in ipairs(SLOT_OFFS) do
                buffItem(readQword(root + soff))
              end
              -- Also refresh classic held-gear pointer chain
              local durA = resolve("Player", {0x228, 0x200, 0x16E8})
              if durA then writeFloat(durA, 99999) end
              local beA = resolve("Player", {0x250, 0x200, 0x16E8})
              if beA then writeInteger(beA, 15) end
              local boA = resolve("Player", {0x254, 0x200, 0x16E8})
              if boA then writeInteger(boA, 15) end
            end
            st.timer.Enabled = true
            print("One-shot weapons: damage x100 + held durability/enhancement freeze")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_gear_oneshot
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            if st and st.dmgAddr and st.dmgPrev then
              writeFloat(st.dmgAddr, st.dmgPrev)
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>302</ID>
              <Description>"[Script] God mode armor (equip durability freeze)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            local function equipRoot()
              local p = readQword(getAddress("Player"))
              if not p or p == 0 then return nil end
              -- Try a few EquipmentComponent-ish offsets on the pawn
              for _, eoff in ipairs({0x16E8, 0x16F0, 0x1700, 0x16E0, 0x1600, 0x1580}) do
                local r = readQword(p + eoff)
                if r and r &gt; 0x10000 then
                  local probe = readQword(r + 0x200)
                  if probe and probe &gt; 0x10000 then return r end
                end
              end
              return readQword(p + 0x16E8)
            end

            local SLOT_OFFS = {
              0x1C0, 0x1C8, 0x1D0, 0x1D8, 0x1E0, 0x1E8, 0x1F0, 0x1F8,
              0x200, 0x208, 0x210, 0x218, 0x220, 0x228, 0x230, 0x238,
              0x240, 0x248, 0x250, 0x258, 0x260, 0x268, 0x270, 0x278
            }

            local function godItem(item)
              if not item or item == 0 or item &lt; 0x10000 then return false end
              local dur = readFloat(item + 0x228)
              if not dur or dur &lt; 0 or dur &gt; 1e8 then
                -- Try alternate durability offsets seen on some Maine builds
                for _, alt in ipairs({0x220, 0x224, 0x22C, 0x230, 0x218}) do
                  local d = readFloat(item + alt)
                  if d and d &gt; 0 and d &lt; 1e7 then
                    writeFloat(item + alt, 99999)
                    return true
                  end
                end
                return false
              end
              writeFloat(item + 0x228, 99999)
              local be = readInteger(item + 0x250)
              local bo = readInteger(item + 0x254)
              if be and be &gt;= 0 and be &lt; 64 then writeInteger(item + 0x250, 15) end
              if bo and bo &gt;= 0 and bo &lt; 64 then writeInteger(item + 0x254, 15) end
              return true
            end

            gg_gear_godarmor = gg_gear_godarmor or {}
            local st = gg_gear_godarmor
            -- Also drop enemy damage scale if GameState resolves (safer armor feel)
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            if getAddressSafe("GameState") then
              st.enemyDmg = resolve("GameState", {0x54, 0x138, 0x388, 0x170})
              if st.enemyDmg then
                st.enemyPrev = readFloat(st.enemyDmg)
                writeFloat(st.enemyDmg, 0.05)
                print(string.format("EnemyDamage scale -&gt; 0.05 (was %.2f)", st.enemyPrev or -1))
              end
            end

            st.timer = createTimer(nil, false)
            st.timer.Interval = 100
            st.timer.OnTimer = function()
              if st.enemyDmg then writeFloat(st.enemyDmg, 0.05) end
              local root = equipRoot()
              if not root then return end
              for _, soff in ipairs(SLOT_OFFS) do
                godItem(readQword(root + soff))
              end
            end
            st.timer.Enabled = true
            print("God armor: equipped durability/enhancement freeze (+ low enemy damage if available)")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_gear_godarmor
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            if st and st.enemyDmg and st.enemyPrev then
              writeFloat(st.enemyDmg, st.enemyPrev)
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>303</ID>
              <Description>"[Script] One-shot + god armor (combo)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end

            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end

            local function equipRoot()
              local p = readQword(getAddress("Player"))
              if not p or p == 0 then return nil end
              for _, eoff in ipairs({0x16E8, 0x16F0, 0x1700, 0x16E0, 0x1600}) do
                local r = readQword(p + eoff)
                if r and r &gt; 0x10000 then
                  local probe = readQword(r + 0x200)
                  if probe and probe &gt; 0x10000 then return r end
                end
              end
              return readQword(p + 0x16E8)
            end

            local SLOT_OFFS = {
              0x1C0, 0x1C8, 0x1D0, 0x1D8, 0x1E0, 0x1E8, 0x1F0, 0x1F8,
              0x200, 0x208, 0x210, 0x218, 0x220, 0x228, 0x230, 0x238,
              0x240, 0x248, 0x250, 0x258, 0x260, 0x268
            }

            local function buffItem(item, attackToo)
              if not item or item == 0 or item &lt; 0x10000 then return end
              local dur = readFloat(item + 0x228)
              if dur and dur &gt;= 0 and dur &lt; 1e8 then
                writeFloat(item + 0x228, 99999)
              else
                for _, alt in ipairs({0x220, 0x224, 0x22C, 0x230}) do
                  local d = readFloat(item + alt)
                  if d and d &gt; 0 and d &lt; 1e7 then writeFloat(item + alt, 99999) end
                end
              end
              local be = readInteger(item + 0x250)
              local bo = readInteger(item + 0x254)
              if be and be &gt;= 0 and be &lt; 64 then writeInteger(item + 0x250, 15) end
              if bo and bo &gt;= 0 and bo &lt; 64 then writeInteger(item + 0x254, 15) end
              if attackToo then
                for _, foff in ipairs({0x240, 0x244, 0x248, 0x24C, 0x258, 0x25C}) do
                  local v = readFloat(item + foff)
                  if v and v &gt; 0.5 and v &lt; 500 then writeFloat(item + foff, 100) end
                end
              end
            end

            gg_gear_combo = gg_gear_combo or {}
            local st = gg_gear_combo
            st.dmgAddr = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
            st.enemyDmg = resolve("GameState", {0x54, 0x138, 0x388, 0x170})
            if st.dmgAddr then
              st.dmgPrev = readFloat(st.dmgAddr)
              writeFloat(st.dmgAddr, 100)
            end
            if st.enemyDmg then
              st.enemyPrev = readFloat(st.enemyDmg)
              writeFloat(st.enemyDmg, 0.05)
            end

            st.timer = createTimer(nil, false)
            st.timer.Interval = 100
            st.timer.OnTimer = function()
              if st.dmgAddr then writeFloat(st.dmgAddr, 100) end
              if st.enemyDmg then writeFloat(st.enemyDmg, 0.05) end
              local root = equipRoot()
              if not root then return end
              for _, soff in ipairs(SLOT_OFFS) do
                buffItem(readQword(root + soff), true)
              end
            end
            st.timer.Enabled = true
            print("Combo: one-shot damage x100 + god armor durability freeze")

            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg_gear_combo
            if st and st.timer then
              st.timer.Enabled = false
              st.timer.destroy()
              st.timer = nil
            end
            if st and st.dmgAddr and st.dmgPrev then writeFloat(st.dmgAddr, st.dmgPrev) end
            if st and st.enemyDmg and st.enemyPrev then writeFloat(st.enemyDmg, st.enemyPrev) end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>304</ID>
              <Description>"Held Durability (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>228</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>305</ID>
              <Description>"Held Base Enhancement (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>250</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>306</ID>
              <Description>"Held Bonus Enhancement (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>4 Bytes</VariableType>
              <Address>Player</Address>
              <Offsets>
                <Offset>254</Offset>
                <Offset>200</Offset>
                <Offset>16E8</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>307</ID>
              <Description>"Player Damage scale (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>50</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>308</ID>
              <Description>"Enemy Damage scale (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>54</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>

        <CheatEntry>
          <ID>72</ID>
          <Description>"World settings (GameState)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00C04080</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>52</ID>
              <Description>"All Recipes Unlocked"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>3A</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>53</ID>
              <Description>"Free Build"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>3B</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>54</ID>
              <Description>"Building Integrity"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>3C</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>55</ID>
              <Description>"All Recipes Free"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>3D</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>56</ID>
              <Description>"Bugs Spawn"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>3E</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>57</ID>
              <Description>"Bugs Ignore Player"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>3F</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>58</ID>
              <Description>"Stamina Drain"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>40</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>59</ID>
              <Description>"Food Spoiling"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>44</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>60</ID>
              <Description>"Pet Invincibility"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>47</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>61</ID>
              <Description>"All Mutations Unlocked"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>49</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>62</ID>
              <Description>"Fully Yoked"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>4A</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>63</ID>
              <Description>"Player Damage (byte toggle)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>41</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>64</ID>
              <Description>"Friendly Fire"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Byte</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>42</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>65</ID>
              <Description>"Player Damage scale"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>50</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>66</ID>
              <Description>"Enemy Damage scale"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>54</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>67</ID>
              <Description>"Time of Day Rate"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>70</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>68</ID>
              <Description>"Hunger Burn Rate"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>74</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>69</ID>
              <Description>"Thirst Burn Rate"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>78</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>70</ID>
              <Description>"Creature Health Scaling"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>80</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
            <CheatEntry>
              <ID>71</ID>
              <Description>"Fall Damage Scaling"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>84</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        
        <CheatEntry>
          <ID>420</ID>
          <Description>"Time of day (freeze / set)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00FFD200</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>421</ID>
              <Description>"README � rate + day float"</Description>
              <Color>00808080</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>{
        Time of day (live)
        ==================
        Primary control: GameState ? � ? Time of Day Rate (+0x70)
          0 = freeze clock
          1 = normal
          20�100 = fast-forward

        Snap dawn/noon/dusk: probes nearby floats for a day-progress
        value (0�1 fractional day or 0�24 hour) and writes presets.
        If snap fails, use save editor Calendar, or Probe TOD floats.
        }
        [ENABLE]
        [DISABLE]
        </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>422</ID>
              <Description>"[Script] Freeze time (rate = 0)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no GameState")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_tod_freeze = gg2_tod_freeze or {}
            local st = gg2_tod_freeze
            st.addr = resolve("GameState", {0x70, 0x138, 0x388, 0x170})
            if not st.addr then
              showMessage("Could not resolve Time of Day Rate")
              error("no TOD rate")
            end
            st.prev = readFloat(st.addr)
            st.timer = createTimer(nil, false)
            st.timer.Interval = 200
            st.timer.OnTimer = function()
              if st.addr then writeFloat(st.addr, 0) end
            end
            st.timer.Enabled = true
            print(string.format("TOD freeze ON (was rate %.3f)", st.prev or -1))
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_tod_freeze
            if st and st.timer then st.timer.Enabled = false; st.timer.destroy(); st.timer = nil end
            if st and st.addr and st.prev then writeFloat(st.addr, st.prev) end
            print("TOD freeze OFF (rate restored)")
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>423</ID>
              <Description>"[Script] Fast-forward time (rate = 50)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no GameState")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_tod_fast = gg2_tod_fast or {}
            local st = gg2_tod_fast
            st.addr = resolve("GameState", {0x70, 0x138, 0x388, 0x170})
            if not st.addr then error("no TOD rate") end
            st.prev = readFloat(st.addr)
            writeFloat(st.addr, 50)
            st.timer = createTimer(nil, false)
            st.timer.Interval = 250
            st.timer.OnTimer = function() if st.addr then writeFloat(st.addr, 50) end end
            st.timer.Enabled = true
            print("TOD fast-forward ON (rate 50)")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_tod_fast
            if st and st.timer then st.timer.Enabled = false; st.timer.destroy(); st.timer = nil end
            if st and st.addr and st.prev then writeFloat(st.addr, st.prev) end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>424</ID>
              <Description>"[Script] Probe TOD floats near rate"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then error("no GameState") end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local rate = resolve("GameState", {0x70, 0x138, 0x388, 0x170})
            if not rate then error("no rate ptr") end
            local base = rate - 0x70
            print(string.format("Settings obj field base %X � dumping floats 0x00..0xC0", base))
            gg2_tod_candidates = {}
            for off = 0, 0xC0, 4 do
              local v = readFloat(base + off)
              if v then
                local kind = ""
                if off == 0x70 then kind = "  &lt;-- Time of Day Rate" end
                if v &gt;= 0 and v &lt;= 1.01 then kind = kind.."  [0..1 frac?]" end
                if v &gt; 1.01 and v &lt;= 24.5 then kind = kind.."  [0..24 hour?]" end
                if v &gt; 24.5 and v &lt; 10000 then kind = kind.."  [day counter?]" end
                print(string.format("  +0x%02X = %.6f%s", off, v, kind))
                if (v &gt;= 0 and v &lt;= 1.01) or (v &gt; 1 and v &lt;= 24.5) or (v &gt; 24.5 and v &lt; 5000 and off ~= 0x70) then
                  table.insert(gg2_tod_candidates, {addr = base + off, off = off, v = v})
                end
              end
            end
            print(string.format("Cached %d TOD-ish candidates in gg2_tod_candidates", #gg2_tod_candidates))
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>425</ID>
              <Description>"[Script] Snap � Dawn (~6:00)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            local HOUR = 6
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local rate = resolve("GameState", {0x70, 0x138, 0x388, 0x170})
            if not rate then error("no GameState TOD") end
            local base = rate - 0x70
            local wrote = 0
            for _, off in ipairs({0x68, 0x6C, 0x60, 0x64, 0x5C, 0x58, 0x74, 0x7C, 0x80}) do
              local a = base + off
              local v = readFloat(a)
              if v then
                if v &gt;= 0 and v &lt;= 1.05 then
                  writeFloat(a, HOUR / 24)
                  print(string.format("Wrote frac day %.3f @ +0x%X (was %.3f)", HOUR/24, off, v))
                  wrote = wrote + 1
                elseif v &gt; 1 and v &lt;= 24.5 then
                  writeFloat(a, HOUR)
                  print(string.format("Wrote hour %.1f @ +0x%X (was %.3f)", HOUR, off, v))
                  wrote = wrote + 1
                elseif v &gt; 24.5 and v &lt; 10000 then
                  local whole = math.floor(v)
                  writeFloat(a, whole + HOUR / 24)
                  print(string.format("Wrote day %.4f @ +0x%X (was %.4f)", whole + HOUR/24, off, v))
                  wrote = wrote + 1
                end
              end
            end
            if wrote == 0 then
              showMessage("No day/hour float found near TOD rate. Run Probe TOD floats, or set Calendar in the save editor.")
            else
              showMessage("Dawn snap wrote "..wrote.." field(s). If sky did not change, Probe TOD and note which offset moves.")
            end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>426</ID>
              <Description>"[Script] Snap � Noon (~12:00)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            local HOUR = 12
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local rate = resolve("GameState", {0x70, 0x138, 0x388, 0x170})
            if not rate then error("no GameState TOD") end
            local base = rate - 0x70
            local wrote = 0
            for _, off in ipairs({0x68, 0x6C, 0x60, 0x64, 0x5C, 0x58, 0x74, 0x7C, 0x80}) do
              local a = base + off
              local v = readFloat(a)
              if v then
                if v &gt;= 0 and v &lt;= 1.05 then
                  writeFloat(a, HOUR / 24); wrote = wrote + 1
                elseif v &gt; 1 and v &lt;= 24.5 then
                  writeFloat(a, HOUR); wrote = wrote + 1
                elseif v &gt; 24.5 and v &lt; 10000 then
                  writeFloat(a, math.floor(v) + HOUR / 24); wrote = wrote + 1
                end
              end
            end
            if wrote == 0 then showMessage("Noon snap: no candidate. Run Probe TOD floats.") else print("Noon snap wrote "..wrote) end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>427</ID>
              <Description>"[Script] Snap � Dusk (~18:00)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            local HOUR = 18
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local rate = resolve("GameState", {0x70, 0x138, 0x388, 0x170})
            if not rate then error("no GameState TOD") end
            local base = rate - 0x70
            local wrote = 0
            for _, off in ipairs({0x68, 0x6C, 0x60, 0x64, 0x5C, 0x58, 0x74, 0x7C, 0x80}) do
              local a = base + off
              local v = readFloat(a)
              if v then
                if v &gt;= 0 and v &lt;= 1.05 then
                  writeFloat(a, HOUR / 24); wrote = wrote + 1
                elseif v &gt; 1 and v &lt;= 24.5 then
                  writeFloat(a, HOUR); wrote = wrote + 1
                elseif v &gt; 24.5 and v &lt; 10000 then
                  writeFloat(a, math.floor(v) + HOUR / 24); wrote = wrote + 1
                end
              end
            end
            if wrote == 0 then showMessage("Dusk snap: no candidate. Run Probe TOD floats.") else print("Dusk snap wrote "..wrote) end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>428</ID>
              <Description>"Time of Day Rate (manual)"</Description>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>Float</VariableType>
              <Address>GameState</Address>
              <Offsets>
                <Offset>70</Offset>
                <Offset>138</Offset>
                <Offset>388</Offset>
                <Offset>170</Offset>
              </Offsets>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>440</ID>
          <Description>"Game speed (time dilation)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00FF8C40</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>441</ID>
              <Description>"README � CustomTimeDilation"</Description>
              <Color>00808080</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>{
        Game speed (live)
        =================
        Writes AActor.CustomTimeDilation-style floats on the Player pawn
        (UE often keeps this near other actor floats). Also probes for a
        global 1.0 dilation near EngineData / world settings.

        0.25 = slow-mo � 1.0 = normal � 2�3 = turbo
        Disable scripts to restore previous values.
        }
        [ENABLE]
        [DISABLE]
        </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>442</ID>
              <Description>"[Script] Probe dilation floats"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local p = readQword(getAddress("Player"))
            if not p or p == 0 then error("null pawn") end
            gg2_dil_addrs = {}
            print(string.format("Scanning Player pawn %X for ~1.0 floats (CustomTimeDilation candidates)�", p))
            for off = 0x80, 0x600, 4 do
              local v = readFloat(p + off)
              if v and math.abs(v - 1.0) &lt; 0.001 then
                print(string.format("  pawn+0x%X = 1.0", off))
                table.insert(gg2_dil_addrs, {kind="pawn", addr=p+off, off=off})
              end
            end
            -- EngineData shallow probe
            if getAddressSafe("EngineData") then
              local e = readQword(getAddress("EngineData"))
              if e and e &gt; 0x10000 then
                print(string.format("EngineData root %X", e))
                for off = 0x0, 0x200, 8 do
                  local o = readQword(e + off)
                  if o and o &gt; 0x10000 and o &lt; 0x7FFFFFFFFFFF then
                    for f = 0, 0x100, 4 do
                      local v = readFloat(o + f)
                      if v and math.abs(v - 1.0) &lt; 0.001 then
                        print(string.format("  Engine+0x%X-&gt;obj+0x%X = 1.0", off, f))
                        table.insert(gg2_dil_addrs, {kind="engine", addr=o+f})
                        if #gg2_dil_addrs &gt; 30 then break end
                      end
                    end
                  end
                  if #gg2_dil_addrs &gt; 30 then break end
                end
              end
            end
            print("Cached "..#gg2_dil_addrs.." candidates in gg2_dil_addrs")
            showMessage("Probe done � check CE Lua console. Then enable Slow-mo / Turbo.")
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>443</ID>
              <Description>"[Script] Slow-mo x0.25"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local MULT = 0.25
            local p = readQword(getAddress("Player"))
            gg2_gamespeed = gg2_gamespeed or {}
            local st = gg2_gamespeed
            st.mult = MULT
            st.targets = {}
            -- Prefer known-ish UE CustomTimeDilation slots + any probed 1.0s
            local tryOffs = {0x98, 0x9C, 0xA0, 0xA4, 0xA8, 0xAC, 0xB0, 0xB4, 0xB8, 0xBC, 0xC0, 0xC4, 0xC8, 0xCC, 0xD0, 0x128, 0x12C, 0x130, 0x134, 0x138, 0x13C, 0x140, 0x148, 0x14C, 0x150}
            for _, off in ipairs(tryOffs) do
              local v = readFloat(p + off)
              if v and math.abs(v - 1.0) &lt; 0.05 then
                table.insert(st.targets, {addr = p + off, prev = v})
              end
            end
            if gg2_dil_addrs then
              for _, c in ipairs(gg2_dil_addrs) do
                local v = readFloat(c.addr)
                if v and v &gt; 0.05 and v &lt; 5 then
                  table.insert(st.targets, {addr = c.addr, prev = v})
                end
              end
            end
            -- dedupe
            local seen, uniq = {}, {}
            for _, t in ipairs(st.targets) do
              if not seen[t.addr] then seen[t.addr]=true; table.insert(uniq, t) end
            end
            st.targets = uniq
            if #st.targets == 0 then
              showMessage("No dilation floats found. Run Probe dilation floats while in-world, then re-enable.")
              error("no dil")
            end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              for _, t in ipairs(st.targets) do writeFloat(t.addr, st.mult) end
            end
            st.timer.Enabled = true
            print(string.format("Game speed x%.2f on %d float(s)", MULT, #st.targets))
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_gamespeed
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.targets then
              for _, t in ipairs(st.targets) do
                if t.prev then pcall(function() writeFloat(t.addr, t.prev) end) end
              end
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>444</ID>
              <Description>"[Script] Turbo x2"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local MULT = 2.0
            local p = readQword(getAddress("Player"))
            gg2_gamespeed2 = gg2_gamespeed2 or {}
            local st = gg2_gamespeed2
            st.mult = MULT
            st.targets = {}
            local tryOffs = {0x98, 0x9C, 0xA0, 0xA4, 0xA8, 0xAC, 0xB0, 0xB4, 0xB8, 0xBC, 0xC0, 0xC4, 0xC8, 0xCC, 0xD0, 0x128, 0x12C, 0x130, 0x134, 0x138, 0x13C, 0x140, 0x148, 0x14C, 0x150}
            for _, off in ipairs(tryOffs) do
              local v = readFloat(p + off)
              if v and math.abs(v - 1.0) &lt; 0.05 then
                table.insert(st.targets, {addr = p + off, prev = v})
              end
            end
            if gg2_dil_addrs then
              for _, c in ipairs(gg2_dil_addrs) do
                local v = readFloat(c.addr)
                if v and v &gt; 0.05 and v &lt; 5 then
                  table.insert(st.targets, {addr = c.addr, prev = v})
                end
              end
            end
            local seen, uniq = {}, {}
            for _, t in ipairs(st.targets) do
              if not seen[t.addr] then seen[t.addr]=true; table.insert(uniq, t) end
            end
            st.targets = uniq
            if #st.targets == 0 then
              showMessage("No dilation floats found. Run Probe dilation floats first.")
              error("no dil")
            end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              for _, t in ipairs(st.targets) do writeFloat(t.addr, st.mult) end
            end
            st.timer.Enabled = true
            print(string.format("Game speed x%.2f on %d float(s)", MULT, #st.targets))
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_gamespeed2
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.targets then
              for _, t in ipairs(st.targets) do
                if t.prev then pcall(function() writeFloat(t.addr, t.prev) end) end
              end
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>445</ID>
              <Description>"[Script] Turbo x3"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local MULT = 3.0
            local p = readQword(getAddress("Player"))
            gg2_gamespeed3 = gg2_gamespeed3 or {}
            local st = gg2_gamespeed3
            st.mult = MULT
            st.targets = {}
            local tryOffs = {0x98, 0x9C, 0xA0, 0xA4, 0xA8, 0xAC, 0xB0, 0xB4, 0xB8, 0xBC, 0xC0, 0xC4, 0xC8, 0xCC, 0xD0, 0x128, 0x12C, 0x130, 0x134, 0x138, 0x13C, 0x140, 0x148, 0x14C, 0x150}
            for _, off in ipairs(tryOffs) do
              local v = readFloat(p + off)
              if v and math.abs(v - 1.0) &lt; 0.05 then
                table.insert(st.targets, {addr = p + off, prev = v})
              end
            end
            if gg2_dil_addrs then
              for _, c in ipairs(gg2_dil_addrs) do
                local v = readFloat(c.addr)
                if v and v &gt; 0.05 and v &lt; 5 then
                  table.insert(st.targets, {addr = c.addr, prev = v})
                end
              end
            end
            local seen, uniq = {}, {}
            for _, t in ipairs(st.targets) do
              if not seen[t.addr] then seen[t.addr]=true; table.insert(uniq, t) end
            end
            st.targets = uniq
            if #st.targets == 0 then
              showMessage("No dilation floats found. Run Probe dilation floats first.")
              error("no dil")
            end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              for _, t in ipairs(st.targets) do writeFloat(t.addr, st.mult) end
            end
            st.timer.Enabled = true
            print(string.format("Game speed x%.2f on %d float(s)", MULT, #st.targets))
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_gamespeed3
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.targets then
              for _, t in ipairs(st.targets) do
                if t.prev then pcall(function() writeFloat(t.addr, t.prev) end) end
              end
            end
            </AssemblerScript>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>500</ID>
          <Description>"Gather / combat helpers"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00C45A2A</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>501</ID>
              <Description>"[Script] One-hit harvest (creature HP scale 0.05 + player dmg x50)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then error("no GameState") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_gather = gg2_gather or {}
            local st = gg2_gather
            st.dmg = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
            st.chp = resolve("GameState", {0x80, 0x138, 0x388, 0x170})
            if st.dmg then st.dmgPrev = readFloat(st.dmg); writeFloat(st.dmg, 50) end
            if st.chp then st.chpPrev = readFloat(st.chp); writeFloat(st.chp, 0.05) end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 200
            st.timer.OnTimer = function()
              if st.dmg then writeFloat(st.dmg, 50) end
              if st.chp then writeFloat(st.chp, 0.05) end
            end
            st.timer.Enabled = true
            print("One-hit harvest/combat ON (PlayerDmg x50, CreatureHP x0.05)")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_gather
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.dmg and st.dmgPrev then writeFloat(st.dmg, st.dmgPrev) end
            if st and st.chp and st.chpPrev then writeFloat(st.chp, st.chpPrev) end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>502</ID>
              <Description>"[Script] Pickup magnet (max haul + stack poke)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_magnet = gg2_magnet or {}
            local st = gg2_magnet
            st.timer = createTimer(nil, false)
            st.timer.Interval = 100
            st.timer.OnTimer = function()
              local stack = resolve("Player", {0x224, 0x200, 0x16E8})
              if stack then
                local v = readInteger(stack)
                if v and v &gt;= 0 and v &lt; 9999 then writeInteger(stack, 9999) end
              end
              if gg2_haul_addr then writeInteger(gg2_haul_addr, 99) end
            end
            st.timer.Enabled = true
            local p = readQword(getAddress("Player"))
            local hits = 0
            for off = 0x100, 0x1200, 8 do
              local c = readQword(p + off)
              if c and c &gt; 0x10000 then
                for f = 0, 0x60, 4 do
                  local v = readInteger(c + f)
                  if v and v &gt;= 5 and v &lt;= 40 then
                    writeInteger(c + f, 99)
                    hits = hits + 1
                    if hits &gt;= 6 then break end
                  end
                end
              end
              if hits &gt;= 6 then break end
            end
            print("Pickup magnet ON � held stack freeze 9999 + bumped "..hits.." haul-ish capacities to 99")
            showMessage("Magnet: held stack frozen at 9999 and a few haul capacity candidates set to 99.\nTrue vacuum radius needs a pickup-range float (run again after picking up loot if haul UI did not update).")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_magnet
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>503</ID>
              <Description>"[Script] XP / Brainpower drip (+5 / sec)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_xp = gg2_xp or {}
            local st = gg2_xp
            st.timer = createTimer(nil, false)
            st.timer.Interval = 1000
            st.timer.OnTimer = function()
              local brain = resolve("Player", {0x2E8, 0x30, 0x1B8})
              local level = resolve("Player", {0x2EC, 0x30, 0x1B8})
              local b2 = resolve("Statistics", {0x1A0})
              local l2 = resolve("Statistics", {0x1A4})
              for _, a in ipairs({brain, b2}) do
                if a then
                  local v = readFloat(a)
                  if v and v &gt;= 0 and v &lt; 1e8 then writeFloat(a, v + 5) end
                  local vi = readInteger(a)
                  if vi and vi &gt;= 0 and vi &lt; 1000000 then writeInteger(a, vi + 5) end
                end
              end
              for _, a in ipairs({level, l2}) do
                if a then
                  local vi = readInteger(a)
                  if vi and vi &gt;= 0 and vi &lt; 100 then writeInteger(a, math.min(99, vi + 1)) end
                end
              end
              if getAddressSafe("Statistics") then
                local s = readQword(getAddress("Statistics"))
                if s and s &gt; 0x10000 then
                  for off = 0x20, 0x200, 4 do
                    local v = readFloat(s + off)
                    if v and v &gt; 10 and v &lt; 1e7 then
                      writeFloat(s + off, v + 5)
                      break
                    end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print("XP/Brainpower drip ON (+5/sec best-effort)")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_xp
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>504</ID>
              <Description>"[Script] Instant craft / free build pack"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then error("no GameState") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_craft = gg2_craft or {}
            local st = gg2_craft
            st.prev = {}
            for _, t in ipairs({
              {n="allRecipes", off={0x3A,0x138,0x388,0x170}},
              {n="freeBuild", off={0x3B,0x138,0x388,0x170}},
              {n="recipesFree", off={0x3D,0x138,0x388,0x170}},
              {n="integrity", off={0x3C,0x138,0x388,0x170}},
            }) do
              local a = resolve("GameState", t.off)
              if a then st.prev[t.n] = readBytes(a, 1, true); writeBytes(a, 1) end
            end
            local p = readQword(getAddressSafe("Player") or 0)
            st.craftFloats = {}
            if p and p ~= 0 then
              for off = 0x100, 0x1400, 8 do
                local c = readQword(p + off)
                if c and c &gt; 0x10000 then
                  for f = 0, 0x80, 4 do
                    local v = readFloat(c + f)
                    if v and v &gt;= 0.4 and v &lt;= 60 then
                      table.insert(st.craftFloats, {addr=c+f, prev=v})
                      writeFloat(c+f, 0.05)
                      if #st.craftFloats &gt;= 8 then break end
                    end
                  end
                end
                if #st.craftFloats &gt;= 8 then break end
              end
            end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 150
            st.timer.OnTimer = function()
              for _, t in ipairs({
                {off={0x3A,0x138,0x388,0x170}},
                {off={0x3B,0x138,0x388,0x170}},
                {off={0x3D,0x138,0x388,0x170}},
              }) do
                local a = resolve("GameState", t.off)
                if a then writeBytes(a, 1) end
              end
              for _, f in ipairs(st.craftFloats or {}) do writeFloat(f.addr, 0.05) end
            end
            st.timer.Enabled = true
            print("Instant craft / free build ON ("..#(st.craftFloats or {}).." time floats nudged)")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local st = gg2_craft
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.prev then
              local map = {
                allRecipes={0x3A,0x138,0x388,0x170},
                freeBuild={0x3B,0x138,0x388,0x170},
                recipesFree={0x3D,0x138,0x388,0x170},
                integrity={0x3C,0x138,0x388,0x170},
              }
              for n, off in pairs(map) do
                local a = resolve("GameState", off)
                if a and st.prev[n] ~= nil then writeBytes(a, st.prev[n]) end
              end
            end
            if st and st.craftFloats then
              for _, f in ipairs(st.craftFloats) do writeFloat(f.addr, f.prev) end
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>505</ID>
              <Description>"[Script] Infinite held uses (durability + stack freeze)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_infuse = gg2_infuse or {}
            local st = gg2_infuse
            st.timer = createTimer(nil, false)
            st.timer.Interval = 80
            st.timer.OnTimer = function()
              local p = getAddressSafe("Player")
              if not p then return end
              local a = readQword(p + 0x16E8)
              if a and a ~= 0 then
                a = readQword(a + 0x200)
                if a and a &gt; 0x10000 then
                  writeFloat(a + 0x228, 99999)
                  local ammo = readInteger(a + 0x224)
                  if ammo and ammo &gt;= 0 and ammo &lt; 100000 then writeInteger(a + 0x224, 9999) end
                  for _, f in ipairs({0x210, 0x214, 0x218, 0x21C, 0x230, 0x234}) do
                    local iv = readInteger(a + f)
                    if iv and iv &gt;= 1 and iv &lt;= 50 then writeInteger(a + f, 99) end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print("Infinite held uses ON")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_infuse
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>506</ID>
              <Description>"[Script] Kill aura (enemy scale 0.01 + player dmg x100)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then error("no GameState") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_aura = gg2_aura or {}
            local st = gg2_aura
            st.pd = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
            st.ed = resolve("GameState", {0x54, 0x138, 0x388, 0x170})
            st.ch = resolve("GameState", {0x80, 0x138, 0x388, 0x170})
            if st.pd then st.pdPrev = readFloat(st.pd); writeFloat(st.pd, 100) end
            if st.ed then st.edPrev = readFloat(st.ed); writeFloat(st.ed, 0.01) end
            if st.ch then st.chPrev = readFloat(st.ch); writeFloat(st.ch, 0.01) end
            st.timer = createTimer(nil, false)
            st.timer.Interval = 150
            st.timer.OnTimer = function()
              if st.pd then writeFloat(st.pd, 100) end
              if st.ed then writeFloat(st.ed, 0.01) end
              if st.ch then writeFloat(st.ch, 0.01) end
            end
            st.timer.Enabled = true
            print("Kill aura ON (global scales � not radius-based)")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local st = gg2_aura
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            if st and st.pd and st.pdPrev then writeFloat(st.pd, st.pdPrev) end
            if st and st.ed and st.edPrev then writeFloat(st.ed, st.edPrev) end
            if st and st.ch and st.chPrev then writeFloat(st.ch, st.chPrev) end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>507</ID>
              <Description>"[Script] Duplicate held stack (set 9999)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local stack = resolve("Player", {0x224, 0x200, 0x16E8})
            if not stack then
              showMessage("Held stack pointer miss. Hold an item and ensure [ACTIVATE] ran.")
              error("no stack")
            end
            local prev = readInteger(stack)
            writeInteger(stack, 9999)
            print(string.format("Held stack %s -&gt; 9999", tostring(prev)))
            showMessage("Held stack set to 9999. Re-open inventory if UI is stale.")
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>508</ID>
              <Description>"[Script] Live unlock pack (recipes / mutations / yoked / freebuild)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then error("no GameState") end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local toggles = {
              {n="All Recipes", off={0x3A,0x138,0x388,0x170}},
              {n="Free Build", off={0x3B,0x138,0x388,0x170}},
              {n="Recipes Free", off={0x3D,0x138,0x388,0x170}},
              {n="All Mutations", off={0x49,0x138,0x388,0x170}},
              {n="Fully Yoked", off={0x4A,0x138,0x388,0x170}},
              {n="Pet Invuln", off={0x47,0x138,0x388,0x170}},
              {n="Bugs Ignore", off={0x3F,0x138,0x388,0x170}},
            }
            local ok = 0
            for _, t in ipairs(toggles) do
              local a = resolve("GameState", t.off)
              if a then writeBytes(a, 1); print("set "..t.n); ok = ok + 1 else print("miss "..t.n) end
            end
            showMessage("Live unlock pack set "..ok.."/"..#toggles.." GameState toggles.\nFog / quests / hatchery / omni tiers still use the save editor.")
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>509</ID>
              <Description>"[Script] Soft freecam (fly + noclip + speed)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then error("no Player") end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            gg2_freecam = gg2_freecam or {}
            local st = gg2_freecam
            local col = resolve("Player", {0x64})
            if col then st.prevCol = readBytes(col, 1, true) end
            local off = gg2_cmc_off or 0xDC8
            st.timer = createTimer(nil, false)
            st.timer.Interval = 50
            st.timer.OnTimer = function()
              local fly = resolve("Player", {0x3A8, off})
              local mode = resolve("Player", {0x178, off})
              local col2 = resolve("Player", {0x64})
              if fly then writeByte(fly, 1) end
              if mode then writeByte(mode, 5) end
              if col2 then writeByte(col2, 0) end
              local p = readQword(getAddressSafe("Player") or 0)
              if p and p ~= 0 then
                local c = readQword(p + off)
                if c and c &gt; 0x10000 then
                  for foff = 0x140, 0x360, 4 do
                    local v = readFloat(c + foff)
                    if v and v &gt; 80 and v &lt; 2500 then
                      writeFloat(c + foff, 3000)
                      break
                    end
                  end
                  for _, goff in ipairs({0x170, 0x174, 0x17C, 0x180}) do
                    local v = readFloat(c + goff)
                    if v and v &gt; 0.2 and v &lt; 5 then writeFloat(c + goff, 0) end
                  end
                end
              end
            end
            st.timer.Enabled = true
            print("Soft freecam ON (player fly/noclip/fast)")
            showMessage("Soft freecam: noclip fly + high speed on the player pawn.\nTrue detached freecam is not wired � use this for scouting.")
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            local function resolve(baseSym, offsets)
              local a = getAddressSafe(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local st = gg2_freecam
            if st and st.timer then st.timer.Enabled=false; st.timer.destroy(); st.timer=nil end
            local off = gg2_cmc_off or 0xDC8
            local fly = resolve("Player", {0x3A8, off})
            local mode = resolve("Player", {0x178, off})
            local col = resolve("Player", {0x64})
            if fly then writeByte(fly, 0) end
            if mode then writeByte(mode, 1) end
            if col then
              if st and st.prevCol ~= nil then writeByte(col, st.prevCol) else writeByte(col, 1) end
            end
            </AssemblerScript>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
        <CheatEntry>
          <ID>200</ID>
          <Description>"G2 adaptive (scan helpers)"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00C08040</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>201</ID>
              <Description>"[Script] Probe vitals near Player"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("Player") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no Player")
            end
            local p = readQword(getAddress("Player"))
            if not p or p == 0 then error("null Player") end
            print(string.format("Player pawn %X — scanning component floats…", p))
            -- Walk likely UObject* slots on the pawn and print health/survival-ish floats.
            local hits = 0
            for off = 0x100, 0x1400, 8 do
              local c = readQword(p + off)
              if c and c &gt; 0x10000 and c &lt; 0x7FFFFFFFFFFF then
                for f = 0, 0x80, 4 do
                  local v = readFloat(c + f)
                  if v and v &gt; 40 and v &lt; 400 then
                    print(string.format("  pawn+0x%X -&gt; obj+0x%X = %.1f", off, f, v))
                    hits = hits + 1
                    if hits &gt; 40 then break end
                  end
                end
              end
              if hits &gt; 40 then break end
            end
            print("Probe done. Use printed addresses with CE 'Add address' if G1 pointer chains look wrong.")
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>202</ID>
              <Description>"[Script] One-shot (PlayerDamage scale x50)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then
              showMessage("Enable [ACTIVATE] first (need GameState AOB)")
              error("no GameState")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            -- Same chain as World settings → Player Damage scale (G1 layout; may need retune on Augusta)
            local addr = resolve("GameState", {0x50, 0x138, 0x388, 0x170})
            if not addr then
              showMessage("Could not resolve Player Damage scale pointer")
              error("no dmg scale")
            end
            gg2_oneshot = gg2_oneshot or {}
            gg2_oneshot.addr = addr
            gg2_oneshot.prev = readFloat(addr)
            writeFloat(addr, 50)
            print(string.format("PlayerDamage scale %.2f -&gt; 50 @ %X", gg2_oneshot.prev or -1, addr))
            [DISABLE]
            {$lua}
            if syntaxcheck then return end
            if gg2_oneshot and gg2_oneshot.addr and gg2_oneshot.prev then
              writeFloat(gg2_oneshot.addr, gg2_oneshot.prev)
            end
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>203</ID>
              <Description>"[Script] Free build + recipes (GameState bytes)"</Description>
              <Color>0000FF00</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>[ENABLE]
            {$lua}
            if syntaxcheck then return end
            if getAddressSafe("GameState") == nil then
              showMessage("Enable [ACTIVATE] first")
              error("no GameState")
            end
            local function resolve(baseSym, offsets)
              local a = getAddress(baseSym)
              if not a then return nil end
              for i = #offsets, 2, -1 do
                a = readQword(a + offsets[i])
                if a == nil or a == 0 then return nil end
              end
              return a + offsets[1]
            end
            local toggles = {
              {n="All Recipes", off={0x3A,0x138,0x388,0x170}},
              {n="Free Build", off={0x3B,0x138,0x388,0x170}},
              {n="Recipes Free", off={0x3D,0x138,0x388,0x170}},
              {n="Perks Unlocked", off={0x49,0x138,0x388,0x170}},
            }
            for _, t in ipairs(toggles) do
              local a = resolve("GameState", t.off)
              if a then
                writeBytes(a, 1)
                print("set "..t.n)
              else
                print("miss "..t.n)
              end
            end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>204</ID>
              <Description>"Editor ↔ CT feature map"</Description>
              <Color>00808080</Color>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>{
        Save editor (tools/grounded2-editor) — offline .csav:
          header rename, vitals, molars/science, gear god/oneshot, mutations,
          quests, tech/analyze, buildings, achievements, calendar, fog,
          chests, omni tiers, buggy tier, hatchery finish, resource/egg presets,
          inventory, multiplayer mirror, OP preset.

        This .CT - live Maine-Win64-Shipping RAM:
          super speed / jump / fly / teleport / bookmarks / GOD LOADOUT,
          buggy mount, TOD freeze/snap, game speed, gather/combat helpers,
          fill/god vitals, hunger/thirst freeze,
          science/molars pointers, held gear, haul,
          GameState free-build / damage scales / time rates,
          adaptive probe + one-shot / free-build scripts.

        Prefer editor for bulk unlocks; prefer .CT for movement while playing.
        }
        [ENABLE]
        [DISABLE]
        </AssemblerScript>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>

        <CheatEntry>
          <ID>76</ID>
          <Description>"DEBUG"</Description>
          <Options moHideChildren="1" moManualExpandCollapse="1"/>
          <Color>00808080</Color>
          <GroupHeader>1</GroupHeader>
          <CheatEntries>
            <CheatEntry>
              <ID>73</ID>
              <Description>"Fetch Base Addresses (AOB)"</Description>
              <VariableType>Auto Assembler Script</VariableType>
              <AssemblerScript>{$lua}
            if syntaxcheck then return end
            [ENABLE]
            print("Fetching Grounded 2 Base Addresses…")
            local aobList = {
            "4C 8B 05 ?? ?? ?? ?? 4D 85 C0 0F 84 ?? ?? ?? ?? 49 8B 80 40 01 00 00 48 89 9C 24 A0 00 00 00 48 85 C0",
            "4C 8B 35 ?? ?? ?? ?? 48 63 05 ?? ?? ?? ?? 4D 8D 24 C6 4D 3B F4",
            "48 8B 05 ?? ?? ?? ?? 48 89 3C D8 48 8B 9C 24 C0 00 00 00 44 88 A7 C0",
            "48 8B 05 ?? ?? ?? ?? 48 8B D1 48 8B 88 F8 0A 00 00 48 85 C9 74 07 48 8B",
            "48 8B 05 ?? ?? ?? ?? 49 8B D3 45 33 C0 48 8B 0A 48 39 81 80 02 00 00 74 11"
            }
            local aobNames = {"Player","Statistics","GearData","EngineData","GameState"}
            local xbase = getAddress(process)
            print(string.format("Base: %X", xbase))
            for i = 1, #aobList do
              local aob = AOBScan(aobList[i], "+X*C*W")
              if aob == nil or aob.Count == 0 then
                print(aobNames[i]..": MISS")
              else
                local instruct = getAddressSafe(aob[0])
                local distance = readInteger(instruct + 3)
                local instructSize = getInstructionSize(instruct)
                local address = (instruct + distance + instructSize) - xbase
                print(string.format("%s: %s+%X", aobNames[i], process, address))
              end
              if aob then aob.destroy() end
            end
            [DISABLE]
            </AssemblerScript>
            </CheatEntry>
            <CheatEntry>
              <ID>74</ID>
              <Description>"Player static (hex)"</Description>
              <ShowAsHex>1</ShowAsHex>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>8 Bytes</VariableType>
              <Address>Player</Address>
            </CheatEntry>
            <CheatEntry>
              <ID>75</ID>
              <Description>"Statistics static (hex)"</Description>
              <ShowAsHex>1</ShowAsHex>
              <ShowAsSigned>0</ShowAsSigned>
              <VariableType>8 Bytes</VariableType>
              <Address>Statistics</Address>
            </CheatEntry>
          </CheatEntries>
        </CheatEntry>
      </CheatEntries>
    </CheatEntry>
  </CheatEntries>
</CheatTable>
